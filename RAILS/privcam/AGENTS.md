# privcam — agent notes

Archived app (`RAILS/apps.yml` → `archived_apps.privcam`). Video upload, `VideosInfiniteScrollReflex`, comments. Port 47200 reserved but not currently deployed. Referenced by the old `deploy_all.sh`; already removed from `master.json`.

- **Deploy script (dormant):** `privcam.sh`. **Recovery source:** `OPERATOR/archive/recovery/installers/privcam.sh`.
- Do not add `privcam` back to active deploy inventories without also updating `master.json`.
