<% content_for :title, t("marketplace.stores.edit", default: "Edit store") %>

<%= tag.section class: "store-form" do %>
  <%= tag.h1 t("marketplace.stores.edit", default: "Edit store") %>
  <%= render "form", store: @store %>
<% end %>
