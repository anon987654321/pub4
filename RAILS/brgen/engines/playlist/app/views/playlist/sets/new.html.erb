<% content_for :title, t("playlist.new_set") %>

<div class="page-header">
  <hgroup>
    <p><%= t("playlist.set_kicker") %></p>
    <%= tag.h1 t("playlist.new_set") %>
  </hgroup>
  <%= link_to "All sets", sets_path, class: "btn btn-ghost btn-sm" %>
</div>

<%= render "form", set: @set %>
