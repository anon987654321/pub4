<% content_for :title, "Edit #{@set.name}" %>

<div class="page-header" role="banner">
  <hgroup>
    <p><%= t("playlist.set_kicker") %></p>
    <%= tag.h1 t("playlist.edit_set") %>
  </hgroup>
  <%= link_to t("playlist.back_to_set"), set_path(@set), class: "btn btn-ghost btn-sm" %>
</div>

<%= render "form", set: @set %>
