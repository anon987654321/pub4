<% href = notification_href(notification) %>
<article id="notification-<%= notification.id %>" role="article" aria-label="<%= t("notifications.aria") %>" class="notification-card">
  <% if href.present? %>
    <h3><%= link_to notification.title, href, class: "notification-title-link" %></h3>
  <% else %>
    <%= tag.h3 notification.title %>
  <% end %>
  <% if notification.body.present? %><%= tag.p notification.body %><% end %>
  <p>
    <%= time_ago(notification.created_at) %>
    <% if notification.read? %> · read<% end %>
  </p>
  <div class="notification-actions cluster">
    <% if href.present? %>
      <%= link_to t("notifications.open"), href, class: "btn btn-ghost btn-sm" %>
    <% end %>
    <% unless notification.read? %>
      <button type="button"
        class="btn btn-ghost btn-sm"
        data-reflex="click->NotificationRead#mark_read"
        data-notification-id="<%= notification.id %>"
        aria-label="<%= t("notifications.mark_as_read") %>">
        <%= t("actions.mark_read") %>
      </button>
    <% end %>
  </div>
</article>
