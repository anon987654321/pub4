<% if live_search_query.present? %>
 <p><%= t("search.results_for", count: @communities.size, query: live_search_query) %></p>
<% end %>

<% if @communities.any? %>
 <%= turbo_frame_tag "communities-list", target: "_top" do %>
 <ul class="community-list" id="communities-list">
 <% @communities.each do |community| %>
   <%= render "communities/card", community: community %>
 <% end %>
 </ul>

 <%= render "shared/infinite_scroll_sentinel",
            pagy: @pagy,
            reflex: "CommunitiesInfiniteScrollReflex#load_more",
            sentinel_id: "communities-sentinel",
            q: params[:q] %>
 <% end %>
<% else %>
 <%= render "shared/empty_state",
       title: t("empty.no_communities"),
       body: t("empty.no_communities_search"),
       action: { label: t("empty.clear_search"), path: communities_path } %>
<% end %>
