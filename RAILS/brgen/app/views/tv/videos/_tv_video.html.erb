<%= render "shared/x_card",
  avatar_url: (tv_video.thumbnail.attached? ? url_for(tv_video.thumbnail) : nil),
  name: tv_video.title,
  handle: tv_video.channel.name,
  meta: "#{tv_video.views_count} views · #{tv_video.duration_formatted}",
  url: tv_video_path(tv_video),
  class: "x-tv" %>