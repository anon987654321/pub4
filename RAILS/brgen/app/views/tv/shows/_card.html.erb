<%= render "shared/x_card",
  avatar_url: nil,
  name: show.title,
  content: truncate(show.description, length: 100),
  meta: "#{show.episodes.count} episodes",
  url: tv_channel_show_path(channel, show),
  class: "x-tv" %>