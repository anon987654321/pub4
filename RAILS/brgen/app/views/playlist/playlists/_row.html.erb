<%= link_to playlist_playlist_path(playlist), class: "radio-library-item playlist-row" do %>
  <strong><%= playlist.name %></strong>
  <span><%= playlist.tracks_count.to_i %> tracks · <%= playlist.plays_count.to_i %> plays</span>
<% end %>