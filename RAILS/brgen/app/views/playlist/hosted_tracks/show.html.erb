<% content_for :title, @track.title %>

<%= render "playlist/playlists/player",
      playlist: @track,
      playlist_name: @track.title,
      tracks_count: 1,
      plays_count: @track.listens.count,
      tracks: [@track],
      active_track: @track %>

<% if authenticated? %>
  <section class="playlist-admin" aria-label="Track management">
    <div class="cluster">
      <%= link_to "Edit", edit_playlist_hosted_track_path(@track), class: "btn btn-ghost btn-sm" %>
      <%= button_to "Delete", playlist_hosted_track_path(@track), method: :delete, data: { turbo_confirm: "Delete?" }, class: "btn btn-danger btn-sm" %>
    </div>
  </section>
<% end %>

<% if @comments.any? %>
  <section class="playlist-details">
    <h2>Timestamped comments</h2>
    <% @comments.each do |comment| %>
      <article>
        <p class="dim"><%= comment.timestamp %>s</p>
        <p><%= comment.body %></p>
      </article>
    <% end %>
  </section>
<% end %>