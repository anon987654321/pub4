<% content_for :title, t("playlist.upload_track") %>

<div class="page-header" role="banner">
  <h1><%= t("playlist.upload_track") %></h1>
  <p class="dim">Direct upload here. High quality 320kbps MP3 / FLAC supported. Replace keeps share links (no URL change).</p>
</div>

<%= render "form", track: @track %>
