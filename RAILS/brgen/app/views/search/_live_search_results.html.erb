<% if @query.present? %>
  <% @results.each do |vertical, records| %>
    <% next if records.blank? %>
    <section>
      <h2><%= vertical.to_s.humanize %></h2>
      <ul>
        <% records.limit(8).each do |record| %>
          <li><%= record.try(:title) || record.try(:name) %></li>
        <% end %>
      </ul>
    </section>
  <% end %>
<% end %>
