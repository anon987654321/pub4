<%= render "shared/x_compose",
      model: Post.new,
      url: posts_path,
      field: :content,
      show_tiptap: true,
      form_actions: "submit->form-submit#lock",
      avatar_glyph: guest? ? "?" : (Current.user.username&.first&.upcase || "B") %>