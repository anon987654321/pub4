<% content_for :title, "Takeaway" %>

<%= render "takeaway/nav_bar", active: "restaurants" %>

<header class="page-header vertical-purpose" role="banner" data-visitor-orientation>
  <h1>Order food nearby</h1>
  <p class="dim">Local kitchens, guest checkout — no signup to place an order.</p>
</header>

<%= live_search_index url: takeaway_restaurants_path, results_partial: "takeaway/restaurants/live_search_results", placeholder: t("search.restaurants"), label: t("search.restaurants_label"), frame_id: "takeaway-restaurants" do |f| %>
  <%= f.select :cuisine, [[t("actions.all_cuisines"), ""]] + Takeaway::Restaurant::CUISINE_TYPES.map { |c| [c, c] }, { selected: params[:cuisine] }, {} %>
<% end %>
