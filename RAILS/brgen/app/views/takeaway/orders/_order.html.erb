<article class="order-row" role="article" aria-label="Order">
  <h2><%= link_to order.restaurant.name, takeaway_order_path(order) %></h2>
  <p class="dim"><%= order.status.humanize %> · <%= order.total_display %> · <time><%= order.created_at.strftime("%d %b %Y") %></time></p>
</article>