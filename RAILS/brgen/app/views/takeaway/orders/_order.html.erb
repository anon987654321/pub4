<%= render "shared/feed_card",
  name: order.restaurant.name,
  meta: "#{order.status.humanize} · #{order.total_display} · #{order.created_at.strftime('%d %b %Y')}",
  url: takeaway_order_path(order),
  class: "takeaway" %>
