# Omakase Ruby styling for Rails (pub4 family)
inherit_gem: { rubocop-rails-omakase: rubocop.yml }

# Large apps exceed omakase Metrics thresholds; gate style without blocking deploy CI.
Metrics:
  Enabled: false
