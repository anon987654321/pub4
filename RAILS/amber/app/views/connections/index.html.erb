<% content_for :title, t("connections.title") %>

<header role="banner" aria-label="<%= t("connections.title") %>">
  <h1><%= t("pages.connections") %></h1>
</header>

<%= render "connections/list" %>

<%= render "shared/pager", pagy: @pagy %>
