<% content_for :title, t("pages.add_item") %>
<header role="banner" aria-label="Add wardrobe item header">
  <%= tag.h1 t("pages.add_item") %>
</header>
<turbo-frame aria-live="polite" id="amber-item-form" data-turbo-action="replace">
<%= render "form", item: @item %>
</turbo-frame>
