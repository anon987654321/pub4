<%# locals: item %>
<% slide_path = item.is_a?(Item) ? demo_wardrobe_item_path(item) : demo_wardrobe_path %>
<div class="wardrobe-slide-card">
  <% if item.is_a?(Item) && item.photos.attached? %>
    <%= link_to slide_path, class: "wardrobe-slide-card__media", aria: { label: item.title } do %>
      <%= responsive_image_tag item.photos.first, alt: item.title, widths: [160, 240], sizes: "140px", class: "wardrobe-slide-card__img" %>
    <% end %>
  <% else %>
    <%= link_to slide_path, class: "wardrobe-slide-card__media wardrobe-slide-card__media--swatch", aria: { label: item.title }, style: "--swatch: #{wardrobe_color_swatch(item.color)};" do %>
      <span class="wardrobe-slide-card__glyph" aria-hidden="true"><%= item.title.to_s.first&.upcase %></span>
    <% end %>
  <% end %>
</div>