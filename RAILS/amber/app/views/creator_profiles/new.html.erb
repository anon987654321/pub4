<% content_for :title, t("creator_profiles.new_title", default: "Create creator profile") %>

<h1><%= t("creator_profiles.new_title", default: "Create creator profile") %></h1>
<p class="dim">Share a public wardrobe showcase others can remix when you allow it.</p>
<%= render "form", profile: @profile %>
