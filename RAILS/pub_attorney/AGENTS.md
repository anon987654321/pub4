# pub_attorney — agent notes

Archived app (`RAILS/apps.yml` → `archived_apps.pub_attorney`). Lawyer/Case/Document models, `CaseMatchReflex`, `wicked_pdf`/`prawn`. Port 10012 reserved but not currently deployed.

- **Deploy script (dormant):** `pub_attorney.sh`. **Recovery source:** `OPERATOR/archive/recovery/installers/pub_attorney.sh`.
- Do not add `pub_attorney` back to active deploy inventories without also updating `master.json`.
