<% content_for :title, "New shift" %>

<div class="page-header">
  <h1>New shift</h1>
  <p class="dim"><%= link_to @volunteer.name, @volunteer %></p>
</div>

<%= render "shifts/form", volunteer: @volunteer, shift: @shift %>