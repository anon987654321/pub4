<% content_for :title, "Box #{@box.week_start}" %>

<div class="page-header" role="banner" aria-label="Box details header">
  <div>
    <p class="dim">Support box</p>
    <h1>Box <%= @box.week_start %></h1>
  </div>
  <%= link_to "All boxes", boxes_path, class: "btn btn-ghost" %>
</div>

<%= render @box %>

<%= render "form", box: @box %>
