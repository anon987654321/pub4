<%# scan: intentional-colors — a brand mark is painted in its own fixed values %>
<div class="jox-logo logo" data-controller="jox-logo" aria-hidden="true">
  <div class="frames">
    <div> <%# scan: intentional — an animation frame, selected by structure, inside an aria-hidden mark %>
      <svg viewBox="0 0 20 50" width="20" height="50">
        <defs>
          <path id="bsd-jox-a" fill="none" stroke="#63c363" stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M10.9 5.15 2 2v40.35L18 48" />
        </defs>
        <use href="#bsd-jox-a" xlink:href="#bsd-jox-a" />
      </svg>
    </div>
    <div> <%# scan: intentional — an animation frame, selected by structure, inside an aria-hidden mark %>
      <svg viewBox="0 0 20 50" width="20" height="50">
        <defs>
          <path id="bsd-jox-b" fill="none" stroke="#4fa34f" stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M9.2 44.85 18 48V7.6L2 2" />
        </defs>
        <use href="#bsd-jox-b" xlink:href="#bsd-jox-b" />
      </svg>
    </div>
    <div> <%# scan: intentional — an animation frame, selected by structure, inside an aria-hidden mark %>
      <svg viewBox="0 0 20 50" width="20" height="50">
        <defs>
          <path id="bsd-jox-c" fill="none" stroke="#00ba7c" stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M9.2 44.85 18 48V7.6L2 2" />
        </defs>
        <use href="#bsd-jox-c" xlink:href="#bsd-jox-c" />
      </svg>
    </div>
  </div>
  <div class="type">BSDports</div>
</div>
