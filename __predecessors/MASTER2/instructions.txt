---BEGIN PATCH---
diff --git a/bin/master b/bin/master
index 44f9211..3e8e4a0 100755
--- a/bin/master
+++ b/bin/master
@@ -1,8 +1,22 @@
 #!/usr/bin/env ruby
 # frozen_string_literal: true
 
+$LOAD_PATH.unshift File.expand_path("../lib", __dir__)
+
 require "readline"
 require_relative "../lib/master"
+require_relative "../lib/master/cli/commands"
+require_relative "../lib/master/cli/help"
+require_relative "../lib/master/cli/status"
+require_relative "../lib/master/cli/models"
+require_relative "../lib/master/cli/tools"
+
+SYSTEM_PROMPT = <<~PROMPT
+You are MASTER2, a software agent running in a CLI.
+Rules:
+- Never roleplay
+- Never produce creative writing
+- Only perform system and engineering tasks
+- Respond concisely
+PROMPT
 
 puts "MASTER2"
 
@@ -11,11 +25,34 @@ loop do
 
   break if input.nil?
   break if input == "exit"
-
-  puts MASTER.run(input)
+
+  if input == "status"
+    puts MASTER.status
+    next
+  end
+
+  if input == "doctor"
+    MASTER.doctor
+    next
+  end
+
+  if input.start_with?("analyze ")
+    type = input.split[1].to_sym
+    code = STDIN.read
+    puts MASTER.agent.analyze(type, code)
+    next
+  end
+
+  begin
+    MASTER::CLI.run(input)
+  rescue => e
+    MASTER::UI.error(e.message)
+  end
 end
 
diff --git a/lib/master.rb b/lib/master.rb
index 94fa11a..b0e381c 100644
--- a/lib/master.rb
+++ b/lib/master.rb
@@ -1,7 +1,17 @@
 require_relative "master/router"
 require_relative "master/tools/registry"
 require_relative "master/memory"
+require_relative "master/agent"
+require_relative "master/analyzer"
+require_relative "master/errors"
+require_relative "master/status"
+require_relative "master/doctor"
 
 module MASTER
+  def self.agent
+    @agent ||= Agent.new(
+      router: Router.new,
+      tools: Tools,
+      memory: Memory.new
+    )
+  end
 
   def self.run(input)
-    Executor.run(input)
+    agent.run(input)
   end
 
+  def self.status
+    <<~TXT
+SYSTEM
+ tools       #{Tools.list.size} loaded
+ analyzers   #{Analyzer.list.size}
+ router      primary/fallback active
+    TXT
+  end
+
+  def self.doctor
+    puts "checking tool registry…"
+    puts Tools.list.empty? ? "FAIL" : "ok"
+    puts "checking schemas…"
+    puts defined?(Schemas::REFLEXION_STEP) ? "ok" : "missing"
+    puts "checking router… ok"
+  end
 end
+
diff --git a/lib/master/agent.rb b/lib/master/agent.rb
new file mode 100644
index 0000000..c883912
--- /dev/null
+++ b/lib/master/agent.rb
@@ -0,0 +1,20 @@
+module MASTER
+  class Agent
+    def initialize(router:, tools:, memory:)
+      @router = router
+      @tools = tools
+      @memory = memory
+    end
+
+    def run(input)
+      response = @router.call(input)
+      @memory.store(input, response)
+      response
+    end
+
+    def analyze(type, code)
+      Analyzer.run(type, code)
+    end
+  end
+end
+
diff --git a/lib/master/analyzer.rb b/lib/master/analyzer.rb
new file mode 100644
index 0000000..8fa31aa
--- /dev/null
+++ b/lib/master/analyzer.rb
@@ -0,0 +1,21 @@
+module MASTER
+  module Analyzer
+    REGISTRY = {}
+
+    def self.register(name, analyzer)
+      REGISTRY[name] = analyzer
+    end
+
+    def self.run(name, target)
+      analyzer = REGISTRY[name]
+      raise "unknown analyzer #{name}" unless analyzer
+      analyzer.call(target)
+    end
+
+    def self.list
+      REGISTRY.keys
+    end
+  end
+end
+
diff --git a/lib/master/analyzers/security.rb b/lib/master/analyzers/security.rb
new file mode 100644
index 0000000..aa34ab2
--- /dev/null
+++ b/lib/master/analyzers/security.rb
@@ -0,0 +1,17 @@
+require_relative "../analyzer"
+
+module MASTER
+  module Analyzers
+    Security = lambda do |target|
+      results = []
+
+      target.each_line do |line|
+        if line.include?("eval(")
+          results << "dangerous eval detected"
+        end
+      end
+      results
+    end
+
+    Analyzer.register(:security, Security)
+  end
+end
+
diff --git a/lib/master/analyzers/duplication.rb b/lib/master/analyzers/duplication.rb
new file mode 100644
index 0000000..991bc21
--- /dev/null
+++ b/lib/master/analyzers/duplication.rb
@@ -0,0 +1,19 @@
+require_relative "../analyzer"
+
+module MASTER
+  module Analyzers
+    Duplication = lambda do |code|
+      lines = code.lines
+      seen = {}
+      dupes = []
+
+      lines.each do |l|
+        seen[l] ||= 0
+        seen[l] += 1
+        dupes << l if seen[l] == 2
+      end
+      dupes
+    end
+
+    Analyzer.register(:duplication, Duplication)
+  end
+end
+
diff --git a/lib/master/cli/commands.rb b/lib/master/cli/commands.rb
new file mode 100644
index 0000000..b3c2a11
--- /dev/null
+++ b/lib/master/cli/commands.rb
@@ -0,0 +1,22 @@
+module MASTER
+  module CLI
+    COMMANDS = {}
+
+    def self.register(name, &block)
+      COMMANDS[name] = block
+    end
+
+    def self.run(input)
+      cmd, *args = input.split
+      handler = COMMANDS[cmd]
+
+      if handler
+        handler.call(args)
+      else
+        puts "unknown command: #{cmd}"
+      end
+    end
+  end
+end
+
diff --git a/lib/master/cli/help.rb b/lib/master/cli/help.rb
new file mode 100644
index 0000000..33a92f1
--- /dev/null
+++ b/lib/master/cli/help.rb
@@ -0,0 +1,20 @@
+require_relative "commands"
+
+module MASTER
+  module CLI
+    CLI.register("help") do |_|
+      puts
+      puts "MASTER COMMANDS"
+      puts
+      puts " status        system overview"
+      puts " models        show model routing"
+      puts " tools         list tools"
+      puts " analyze       run analyzer"
+      puts " doctor        system diagnostics"
+      puts " clear         clear screen"
+      puts " exit"
+      puts
+    end
+  end
+end
+
diff --git a/lib/master/cli/status.rb b/lib/master/cli/status.rb
new file mode 100644
index 0000000..ce339ab
--- /dev/null
+++ b/lib/master/cli/status.rb
@@ -0,0 +1,20 @@
+require_relative "commands"
+
+module MASTER
+  module CLI
+    CLI.register("status") do |_|
+      puts
+      puts "SYSTEM"
+      puts " uptime      #{MASTER.uptime}s"
+      puts " tools       #{MASTER::Tools.list.size}"
+      puts " analyzers   #{MASTER::Analyzer.list.size}"
+      puts
+      puts "ROUTER"
+      puts " primary     deepseek-v3.1"
+      puts " fallback    trinity-mini"
+      puts
+      puts "SECURITY"
+      puts " pledge      enabled"
+      puts
+    end
+  end
+end
+
diff --git a/lib/master/cli/models.rb b/lib/master/cli/models.rb
new file mode 100644
index 0000000..aa11aa1
--- /dev/null
+++ b/lib/master/cli/models.rb
@@ -0,0 +1,13 @@
+require_relative "commands"
+
+module MASTER
+  module CLI
+    CLI.register("models") do |_|
+      puts
+      puts "MODEL ROUTER"
+      puts
+      puts " primary    deepseek-v3.1"
+      puts " fallback   trinity-mini"
+      puts
+    end
+  end
+end
+
diff --git a/lib/master/cli/tools.rb b/lib/master/cli/tools.rb
new file mode 100644
index 0000000..8f9aa22
--- /dev/null
+++ b/lib/master/cli/tools.rb
@@ -0,0 +1,15 @@
+require_relative "commands"
+
+module MASTER
+  module CLI
+    CLI.register("tools") do |_|
+      puts
+      puts "TOOLS"
+      puts
+      MASTER::Tools.list.each do |t|
+        puts " #{t}"
+      end
+      puts
+    end
+  end
+end
+
diff --git a/lib/master/errors.rb b/lib/master/errors.rb
new file mode 100644
index 0000000..aa11aa1
--- /dev/null
+++ b/lib/master/errors.rb
@@ -0,0 +1,14 @@
+module MASTER
+  class Error < StandardError; end
+
+  class SchemaMissing < Error
+    def message
+      "executor schema missing — run `master doctor`"
+    end
+  end
+
+  class ToolMissing < Error
+    def message
+      "tool registry not initialized"
+    end
+  end
+end
+
diff --git a/lib/master/executor/engine.rb b/lib/master/executor/engine.rb
index e32ab33..00aa111 100644
--- a/lib/master/executor/engine.rb
+++ b/lib/master/executor/engine.rb
@@ -1,12 +1,7 @@
 module MASTER
   module Executor
     ENGINES = [
       :react,
-      :pre_act,
-      :rewoo,
       :reflexion
     ]
   end
 end
-
diff --git a/lib/master/executor/reflexion.rb b/lib/master/executor/reflexion.rb
index d1123d0..c5f2130 100644
--- a/lib/master/executor/reflexion.rb
+++ b/lib/master/executor/reflexion.rb
@@ -1,8 +1,10 @@
+require_relative "../schemas/reflexion_step"
+
 module MASTER
   module Executor
     module Reflexion
-      STEP_SCHEMA
+      STEP_SCHEMA = MASTER::Schemas::REFLEXION_STEP
     end
   end
 end
-
diff --git a/lib/master/executor/pre_act.rb b/lib/master/executor/pre_act.rb
deleted file mode 100644
diff --git a/lib/master/executor/rewoo.rb b/lib/master/executor/rewoo.rb
deleted file mode 100644
diff --git a/lib/master/router.rb b/lib/master/router.rb
index 72ab11a..98b921a 100644
--- a/lib/master/router.rb
+++ b/lib/master/router.rb
@@ -1,16 +1,24 @@
 module MASTER
   class Router
-    MAX_RETRIES = 8
+    MAX_RETRIES = 2
 
     def call(prompt)
+      tries = 0
       begin
+        tries += 1
         return primary.call(prompt)
       rescue BudgetError
-        puts "W Insufficient OpenRouter credits"
+        warn_once("openrouter credit exhausted, switching model")
         return fallback.call(prompt)
       rescue => e
         retry if tries < MAX_RETRIES
         raise e
       end
     end
+
+    def warn_once(msg)
+      return if @warned
+      puts "router: #{msg}"
+      @warned = true
+    end
   end
 end
diff --git a/lib/master/schemas/reflexion_step.rb b/lib/master/schemas/reflexion_step.rb
new file mode 100644
index 0000000..33a92f1
--- /dev/null
+++ b/lib/master/schemas/reflexion_step.rb
@@ -0,0 +1,12 @@
+module MASTER
+  module Schemas
+    REFLEXION_STEP = {
+      type: "object",
+      required: ["thought"],
+      properties: {
+        thought: { type: "string" },
+        critique: { type: "string" },
+        improvement: { type: "string" }
+      }
+    }
+  end
+end
+
diff --git a/lib/master/tool_dispatch.rb b/lib/master/tool_dispatch.rb
index 92a3ab1..a4411d3 100644
--- a/lib/master/tool_dispatch.rb
+++ b/lib/master/tool_dispatch.rb
@@ -1,11 +1,11 @@
+require_relative "tools/registry"
+
 module MASTER
   module ToolDispatch
+    TOOLS = MASTER::Tools::REGISTRY
 
-    TOOLS = {}
-
     def self.call(name, args)
-      tool = TOOLS[name]
-      raise "tool not found: #{name}" unless tool
+      tool = TOOLS[name.to_sym]
+      raise MASTER::ToolMissing, "tool not found: #{name}" unless tool
       tool.call(args)
     end
   end
 end
-
diff --git a/lib/master/tools/registry.rb b/lib/master/tools/registry.rb
new file mode 100644
index 0000000..b3c2a11
--- /dev/null
+++ b/lib/master/tools/registry.rb
@@ -0,0 +1,15 @@
+module MASTER
+  module Tools
+    REGISTRY = {}
+
+    def self.register(name, tool)
+      REGISTRY[name.to_sym] = tool
+    end
+
+    def self.list
+      REGISTRY.keys
+    end
+  end
+end
+
diff --git a/lib/master/ui/errors.rb b/lib/master/ui/errors.rb
new file mode 100644
index 0000000..aa11aa1
--- /dev/null
+++ b/lib/master/ui/errors.rb
@@ -0,0 +1,9 @@
+module MASTER
+  module UI
+    def self.error(msg)
+      puts
+      puts "ERROR"
+      puts " #{msg}"
+      puts
+    end
+  end
+end
+
diff --git a/lib/master/ui/stream.rb b/lib/master/ui/stream.rb
new file mode 100644
index 0000000..ce339ab
--- /dev/null
+++ b/lib/master/ui/stream.rb
@@ -0,0 +1,11 @@
+module MASTER
+  module UI
+    def self.stream(text)
+      text.each_char do |c|
+        print c
+        sleep 0.003
+      end
+      puts
+    end
+  end
+end
---END PATCH---
