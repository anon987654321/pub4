description: Deliberately invalid. The tests assert this is refused.
stages:
  - name: first
    model: black-forest-labs/flux-schnell
    prompt: anything
    inherits: [image]
  - name: second
    model: black-forest-labs/flux-schnell
    prompt: hand me the previous frame
    inherits: [image]
    options:
      safety_tolerance: 3
