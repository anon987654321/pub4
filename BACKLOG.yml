# pub4 backlog — single source for open operator work (replaces MASTER/TODO.md + DEPLOY/TODO.md).
# Horizon/aspirational features: DEPLOY/rails/apps.horizon.yml (agent: ignore).

open: []

completed_summary:
  master:
    - Constitution self-test gate, data singularity lint, Converge removal
    - WebGL deferred-boot guards + browser CI profile
    - bin/handoff, bin/todo-retire, two-spine documentation
    - Master::Reach::Exec bounded subprocess migration
    - Master::Kernel:: namespace spine
  deploy:
    - schema_migration, asset_freshness, human_walkthrough gates
    - brgen/amber/bsdports/hjerterom backlog items landed
    - apps.yml reconciled (0 port/missing in active apps)
    - Operator accommodation layer (START_HERE, contracts, playbooks)
  operator:
    - Environment-aware gates, bin/pub4 status, vps-state/deploy/logs
    - Contributor check profiles, RECIPES.md, per-app AGENTS.md

references:
  active_features: DEPLOY/rails/apps.yml
  horizon_features: DEPLOY/rails/apps.horizon.yml
  debt: DEPLOY/DEBT.md
  decisions: DEPLOY/DECISIONS.md