# BA103

**Spec:** BA103 Wordmark click: tapping wordmark on mobile scrolls to top + resets nav to hidden state; on desktop links to `/`; never navigates away when already on root

**Artifact:** `DEPLOY/artifacts/brgen/BA103.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
