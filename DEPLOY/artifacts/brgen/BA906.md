# BA906

**Spec:** BA906 New posts indicator: when CableReady broadcasts new post to `FeedChannel`, show "3 nye innlegg" pill at top of feed (like X's "N new tweets"); click → scroll to top + refresh; never auto-inject into feed (disrupts reading)

**Artifact:** `DEPLOY/artifacts/brgen/BA906.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
