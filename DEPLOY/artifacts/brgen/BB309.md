# BB309

**Spec:** BB309 Listening party: room-based synchronized playback; `ListeningParty(playlist_id, host_id, started_at, current_track_position)`; all party members sync to host's track position via ActionCable heartbeat every 2s; max 50 members per party

**Artifact:** `DEPLOY/artifacts/brgen/BB309.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
