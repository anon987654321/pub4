# BA1306

**Spec:** BA1306 No render-blocking on landing: zero `<script>` tags in `<head>` on landing layout; all JS `defer`; Stimulus connects after HTML painted; landing is usable before JS loads (wordmark + arrow visible immediately)

**Artifact:** `DEPLOY/artifacts/brgen/BA1306.md`

## Status

Deployable artifact tracked in repo.
