# BB307

**Spec:** BB307 Radio mode: "Radio basert på" — seeds from last 5 tracks; calls MASTER tool `music_recommend` which queries Last.fm similar tracks API; fills queue with 20 tracks; refreshes 5 tracks before queue exhausts; infinite radio feel

**Artifact:** `DEPLOY/artifacts/brgen/BB307.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
