# BA1103

**Spec:** BA1103 Moderation pipeline: `AnonModerationJob` — synchronous for anonymous posts (user waits max 2s); if LLM response takes >2s, approve optimistically + queue async re-check; reject immediately if sync check returns `approved: false`

**Artifact:** `DEPLOY/artifacts/brgen/BA1103.md`

## Status

Deployable artifact tracked in repo.
