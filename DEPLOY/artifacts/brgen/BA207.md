# BA207

**Spec:** BA207 Tilt gesture: `deviceorientation` event; if `beta > 25` (device tilted forward >25°) and user has been on page >2s → `this.showNav()`; requires `DeviceOrientationEvent.requestPermission()` on iOS 13+; request on first tap

**Artifact:** `DEPLOY/artifacts/brgen/BA207.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
