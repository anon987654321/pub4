# BA405

**Spec:** BA405 City launch checklist: new city requires: subdomain DNS + TLS cert (wildcard covers *.citynet.no), City record in DB, seed content batch, relayd relay rule, rcctl enable for city process (or shared process with tenant routing)

**Artifact:** `DEPLOY/artifacts/brgen/BA405.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
