# BA213

**Spec:** BA213 ARIA: `<nav aria-label="Vertikaler" aria-hidden="true" data-landing-target="nav">` at rest; `aria-hidden="false"` when open; focus trapped inside when open via `focus-trap-js` or manual `tabindex` management

**Artifact:** `DEPLOY/artifacts/brgen/BA213.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
