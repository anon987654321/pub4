# BA611

**Spec:** BA611 Camera: `data-action="click->composer#openCamera"` — `<input type="file" accept="image/*,video/*" capture="environment">` on mobile triggers native camera; on desktop opens file picker; multiple files allowed

**Artifact:** `DEPLOY/artifacts/brgen/BA611.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
