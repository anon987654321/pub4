# BB502

**Spec:** BB502 Message model: `Message(conversation_id, sender_id, body, kind: text|image|file|reaction, parent_id, delivered_at, read_at, edited_at, deleted_at)`; soft delete — `deleted_at` set, body replaced with "Slettet melding"; parent_id for thread replies

**Artifact:** `DEPLOY/artifacts/brgen/BB502.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
