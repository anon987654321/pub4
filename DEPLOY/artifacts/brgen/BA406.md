# BA406

**Spec:** BA406 City-specific domain aliases: brgen.no maps to Bergen; each top-level city brand domain resolves to its city; `citynet.no` subdomains as fallback for unlaunched cities during beta

**Artifact:** `DEPLOY/artifacts/brgen/BA406.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
