# BA1008

**Spec:** BA1008 Tiptap → ActionText: on form submit, serialize Tiptap JSON to HTML via `editor.getHTML()`; write to hidden `<input name="post[body]">` which ActionText reads; ActionText sanitizes on server before storage

**Artifact:** `DEPLOY/artifacts/brgen/BA1008.md`

## Status

Deployable artifact tracked in repo.
