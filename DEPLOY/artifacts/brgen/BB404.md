# BB404

**Spec:** BB404 Order model: `Order(user_id, restaurant_id, status, subtotal_ore, delivery_fee_ore, tip_ore, total_ore, delivery_address_json, special_instructions, estimated_delivery_at)`; status enum: pending → confirmed → preparing → out_for_delivery → delivered | cancelled

**Artifact:** `DEPLOY/artifacts/brgen/BB404.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
