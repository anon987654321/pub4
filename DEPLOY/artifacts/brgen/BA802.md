# BA802

**Spec:** BA802 Post card: `<article class="post-card" data-controller="post">` — `background: rgba(255,255,255,0.03); border: 1px solid rgba(255,255,255,0.08); border-radius: 12px; padding: var(--space-4); margin-bottom: var(--space-3); transition: border-color var(--duration-fast)` — ghost card on black; hover: border-color to `rgba(255,255,255,0.18)`

**Artifact:** `DEPLOY/artifacts/brgen/BA802.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
