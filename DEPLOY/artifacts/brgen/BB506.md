# BB506

**Spec:** BB506 Typing indicator: `channel.perform "typing"` on keypress (debounced 500ms); server broadcasts `typing_start` to other participants; Stimulus shows "skriver..." ephemeral indicator; auto-clears after 3s without new event

**Artifact:** `DEPLOY/artifacts/brgen/BB506.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
