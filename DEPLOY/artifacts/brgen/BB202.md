# BB202

**Spec:** BB202 HLS streaming: `hls.js` loaded via importmap; `Hls.isSupported()` → hls.js; else `video.src = m3u8` (Safari native HLS); `HLS_URL` per channel from Rails config; streams from Cloudflare Stream or self-hosted nginx-rtmp (future)

**Artifact:** `DEPLOY/artifacts/brgen/BB202.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
