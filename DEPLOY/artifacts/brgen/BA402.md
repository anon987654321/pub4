# BA402

**Spec:** BA402 City detection middleware: `CityDetectionMiddleware` reads `request.subdomain`; looks up `City.find_by(subdomain: subdomain)`; sets `ActsAsTenant.current_tenant`; 404s on unknown subdomain; no cross-city leakage possible at the SQL layer

**Artifact:** `DEPLOY/artifacts/brgen/BA402.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
