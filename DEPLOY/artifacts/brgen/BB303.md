# BB303

**Spec:** BB303 Audio player: sticky bottom bar (desktop) / fullscreen player (mobile); Stimulus `player-controller.js` manages `<audio>` element; play/pause, seek, volume, skip; next track on `ended` event; queue is playlist tracks starting at selected index

**Artifact:** `DEPLOY/artifacts/brgen/BB303.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
