# BB503

**Spec:** BB503 Real-time delivery: `MessagesChannel` subscribed per-conversation; `Message.after_create_commit` broadcasts CableReady `append` to conversation stream; recipient's unread badge increments via separate `NotificationsChannel` broadcast

**Artifact:** `DEPLOY/artifacts/brgen/BB503.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
