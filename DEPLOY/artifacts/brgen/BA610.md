# BA610

**Spec:** BA610 Microphone: `data-action="click->composer#toggleRecord"` — Web Audio API MediaRecorder; records voice note as MP3 via `lamejs` or WebM; Active Storage direct upload; voice note player rendered inline in post

**Artifact:** `DEPLOY/artifacts/brgen/BA610.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
