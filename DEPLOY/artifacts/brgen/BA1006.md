# BA1006

**Spec:** BA1006 Code block with language: Tiptap `CodeBlockLowlight` extension with `lowlight` for syntax highlighting; language selector dropdown on focus; renders `<pre><code class="language-ruby">` in post body

**Artifact:** `DEPLOY/artifacts/brgen/BA1006.md`

## Status

Deployable artifact tracked in repo.
