# BB505

**Spec:** BB505 Read receipts: `MessagesChannel` receives `read` event when recipient scrolls message into viewport (`IntersectionObserver`); `PATCH /messages/:id/read` sets `read_at`; sender sees double-tick → blue-tick CSS class swap

**Artifact:** `DEPLOY/artifacts/brgen/BB505.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
