# BB102

**Spec:** BB102 Swipe gesture: Stimulus `swipe-controller.js` — `pointerdown` captures start X; `pointermove` applies `translateX(delta) rotate(delta/20deg)` live; `pointerup` with |delta|>80px commits like/pass; with |delta|<80px springs back via CSS transition

**Artifact:** `DEPLOY/artifacts/brgen/BB102.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
