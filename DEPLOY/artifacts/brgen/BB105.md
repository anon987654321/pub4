# BB105

**Spec:** BB105 Distance filter: `acts_as_tenant` scopes to city, but within-city distance uses `ST_Distance` on PostGIS-style lat/lon stored as REAL columns; slider 1–50km; `WHERE distance(lat, lon, :my_lat, :my_lon) <= :km` via SQLite custom function registered at boot

**Artifact:** `DEPLOY/artifacts/brgen/BB105.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
