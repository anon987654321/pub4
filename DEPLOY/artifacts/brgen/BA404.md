# BA404

**Spec:** BA404 Inter-city isolation test: CI test verifies that `Post.create(city_id: city_a.id)` is NOT findable when tenant = city_b; hard assertion; any regression fails CI immediately

**Artifact:** `DEPLOY/artifacts/brgen/BA404.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
