# BA814

**Spec:** BA814 Report: `data-action="click->post#report"` → bottom sheet (mobile) or popover (desktop) with categories: Spam, Hatefullt innhold, Feil informasjon, Upassende, Annet; submits `POST /reports`; confirmation: "Innmeldt. Vi ser på det."

**Artifact:** `DEPLOY/artifacts/brgen/BA814.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
