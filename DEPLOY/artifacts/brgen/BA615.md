# BA615

**Spec:** BA615 Character count: `<span data-composer-target="charCount">` in bottom-right of composer; shows remaining characters (10000 - current); turns amber at 500 remaining, red at 100; Tiptap CharacterCount extension provides count

**Artifact:** `DEPLOY/artifacts/brgen/BA615.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
