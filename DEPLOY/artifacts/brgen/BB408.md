# BB408

**Spec:** BB408 Delivery driver (future): `Driver` model; `OrderAssignment`; driver app (PWA) with geolocation tracking; customer sees driver on map; `DriverLocationChannel` broadcasts GPS every 5s; deferred — requires driver recruitment

**Artifact:** `DEPLOY/artifacts/brgen/BB408.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
