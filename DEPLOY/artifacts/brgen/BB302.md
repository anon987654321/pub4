# BB302

**Spec:** BB302 Track model: `Track(title, artist, album, duration_seconds, isrc, spotify_uri, youtube_id, soundcloud_id, audio_url)`; ISRCs deduplicate across sources; `audio_url` is a self-hosted preview URL (30s MP3) from whatever source resolves first

**Artifact:** `DEPLOY/artifacts/brgen/BB302.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
