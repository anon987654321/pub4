# BA210

**Spec:** BA210 Nav dismiss: tap outside nav (on the underlying page content scrim), press `Escape`, or swipe-up while nav open → `this.hideNav()` with reversed spring; `transform: translateY(-100%)` returns nav to hidden

**Artifact:** `DEPLOY/artifacts/brgen/BA210.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
