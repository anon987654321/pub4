# BB207

**Spec:** BB207 Reaction bar: floating emoji row (❤️🔥😂👏🤔) below player; tap → emoji flies up in CSS animation (`@keyframes fly-up { to { transform: translateY(-80px); opacity: 0 } }`); `POST /tv/reactions` broadcasts count via CableReady; reaction counters update live

**Artifact:** `DEPLOY/artifacts/brgen/BB207.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
