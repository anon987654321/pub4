# BA809

**Spec:** BA809 Action icons: heart (like), star (save), share-box (share), code-brackets (embed), chat-bubble (reply), flag (report) — Heroicons outline at 18px; hover → `rgba(255,255,255,0.9)` + icon-specific colour (heart→pink, star→amber); spring scale `1.15` on click

**Artifact:** `DEPLOY/artifacts/brgen/BA809.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
