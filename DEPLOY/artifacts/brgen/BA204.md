# BA204

**Spec:** BA204 Nav panel: `<nav data-landing-target="nav" class="slide-nav">` with `position: fixed; inset-inline: 0; top: 0; background: #000; transform: translateY(-100%); transition: transform 0.45s cubic-bezier(0.32, 0.72, 0, 1); z-index: var(--z-overlay); padding: clamp(60px, 10vw, 80px) clamp(20px, 5vw, 48px) clamp(32px, 6vw, 48px)` — slides from top; covers full viewport; `cubic-bezier(0.32, 0.72, 0, 1)` = iOS sheet spring

**Artifact:** `DEPLOY/artifacts/brgen/BA204.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
