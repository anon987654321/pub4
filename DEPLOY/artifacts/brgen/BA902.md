# BA902

**Spec:** BA902 Trending score: `score = (likes + comments * 2 + shares * 3) / ((hours_since_post + 2) ** 1.8)` — HN gravity formula; recomputed by `ScorePostsJob` every 10 minutes; stored in `posts.trending_score` for fast sort

**Artifact:** `DEPLOY/artifacts/brgen/BA902.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
