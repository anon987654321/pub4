# BB104

**Spec:** BB104 Match modal: full-screen overlay on mutual match; both user avatars animate toward center (CSS `keyframes slide-in-left/right`); "Det er en match!" headline; two CTAs — "Send melding" (opens chat) and "Fortsett å sveipe" (dismisses); modal auto-dismisses after 6s if untouched

**Artifact:** `DEPLOY/artifacts/brgen/BB104.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
