# BA904

**Spec:** BA904 Chronological option: `?sort=new` URL param serves pure reverse-chronological feed; no algorithm; user preference toggled via Stimulus; stored in `current_user.feed_sort` preference

**Artifact:** `DEPLOY/artifacts/brgen/BA904.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
