# BB103

**Spec:** BB103 Like/pass decision: `POST /dating/likes` with `{target_id:, direction: "like"|"pass"}`; `DatingLike` model with dedup via `UNIQUE(liker_id, liked_id)`; mutual like → create `DatingMatch`; Turbo Stream triggers match modal

**Artifact:** `DEPLOY/artifacts/brgen/BB103.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
