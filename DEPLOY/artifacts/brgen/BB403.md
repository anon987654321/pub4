# BB403

**Spec:** BB403 Cart via session: cart stored in encrypted Rails session (Solid Cache backed); `cart = {restaurant_id:, items: [{item_id:, quantity:, notes:}]}`; cross-restaurant add → prompt "Start ny ordre?" modal; cart clears on order placed

**Artifact:** `DEPLOY/artifacts/brgen/BB403.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
