# BA706

**Spec:** BA706 Anon rate limiting: Rack::Attack rule — max 2 POST to `/posts` per 10 minutes per IP when `user_id: nil`; harder limit than the 7-day soft limit; prevents scripted spam despite fingerprint bypass

**Artifact:** `DEPLOY/artifacts/brgen/BA706.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
