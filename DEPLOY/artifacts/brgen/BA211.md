# BA211

**Spec:** BA211 Scrim: when nav open, `<div class="nav-scrim" data-action="click->landing#hideNav">` at `position: fixed; inset: 0; background: rgba(0,0,0,0.4); z-index: calc(var(--z-overlay) - 1); backdrop-filter: blur(2px)` — tap scrim = dismiss; blur creates depth separation

**Artifact:** `DEPLOY/artifacts/brgen/BA211.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
