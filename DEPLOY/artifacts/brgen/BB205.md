# BB205

**Spec:** BB205 Chat alongside stream: `TvChatChannel` ActionCable; right panel chat (desktop) / bottom sheet (mobile); messages scroll up; max 200 messages in DOM (older removed); rate limit 1 msg/5s per user; MASTER moderates chat in background

**Artifact:** `DEPLOY/artifacts/brgen/BB205.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
