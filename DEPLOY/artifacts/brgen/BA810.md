# BA810

**Spec:** BA810 Like animation: click heart → `animation: heartbeat 0.4s cubic-bezier(0.34, 1.56, 0.64, 1)` → fill colour `#f43f5e`; count increments via Turbo Stream broadcast (not optimistic — real count); unlike reverses

**Artifact:** `DEPLOY/artifacts/brgen/BA810.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
