# BA813

**Spec:** BA813 Reply inline: reply button expands a sub-composer inline below the post (not a page navigation); same Tiptap composer, smaller; anonymous option if not logged in; submit via `POST /posts/:id/comments`; new comment appended via Turbo Stream

**Artifact:** `DEPLOY/artifacts/brgen/BA813.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
