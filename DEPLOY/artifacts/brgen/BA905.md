# BA905

**Spec:** BA905 Infinite scroll: IntersectionObserver on sentinel div at bottom of feed; on intersection, Turbo Frame `src` updated with next page cursor; new posts appended via `turbo_stream.append`; scroll position preserved

**Artifact:** `DEPLOY/artifacts/brgen/BA905.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
