# BA104

**Spec:** BA104 Full-bleed void: `position: fixed; inset: 0; background: #000` on `:root` — even momentum scroll overshoot is black; no white flash from browser chrome; `color-scheme: dark` on `<html>` so browser renders scrollbars dark

**Artifact:** `DEPLOY/artifacts/brgen/BA104.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
