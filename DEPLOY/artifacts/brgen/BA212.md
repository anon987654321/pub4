# BA212

**Spec:** BA212 First-visit persistence: `localStorage.setItem("nav-discovered", "1")` once user opens nav; on subsequent visits, show arrow at 20% opacity (subtler) rather than animated — user already knows the gesture

**Artifact:** `DEPLOY/artifacts/brgen/BA212.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
