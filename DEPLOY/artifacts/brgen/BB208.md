# BB208

**Spec:** BB208 Offline notice: service worker caches EPG and last-known channel metadata; if stream fails, show "Sender ikke akkurat nå — neste sending: [time]"; EPG fallback from cache; no blank screen

**Artifact:** `DEPLOY/artifacts/brgen/BB208.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
