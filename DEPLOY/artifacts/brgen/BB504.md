# BB504

**Spec:** BB504 Message input: Stimulus `chat-input-controller.js`; `textarea` auto-grows (rows 1→6 max); `Enter` sends, `Shift+Enter` newline; `POST /conversations/:id/messages` Turbo Stream appends optimistically before server confirm; rollback on error

**Artifact:** `DEPLOY/artifacts/brgen/BB504.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
