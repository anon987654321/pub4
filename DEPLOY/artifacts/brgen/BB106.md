# BB106

**Spec:** BB106 Age filter: two-thumb range slider (Stimulus `range-slider-controller.js`); `min_age` + `max_age` params; birthday stored, age derived via `(julianday('now') - julianday(birthday)) / 365.25`; CHECK constraint: age >= 18

**Artifact:** `DEPLOY/artifacts/brgen/BB106.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
