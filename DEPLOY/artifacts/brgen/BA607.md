# BA607

**Spec:** BA607 Tiptap slash commands: type `/` → dropdown of insert commands: `/image` (upload), `/code` (code block), `/quote` (blockquote), `/poll` (poll), `/link` (embed link with preview); custom `Extension` that listens for `/` + word

**Artifact:** `DEPLOY/artifacts/brgen/BA607.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
