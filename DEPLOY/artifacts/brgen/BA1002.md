# BA1002

**Spec:** BA1002 Link unfurl: on URL paste into editor, `POST /link_previews?url=` fetches OG metadata; renders link card below URL text: title, description, thumbnail, domain; user can dismiss card; stored as `<a data-type="link-preview" ...>` node

**Artifact:** `DEPLOY/artifacts/brgen/BA1002.md`

## Status

Deployable artifact tracked in repo.
