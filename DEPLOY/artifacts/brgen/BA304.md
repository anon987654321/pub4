# BA304

**Spec:** BA304 Swipe-left affordance: after 1.5s with nav open, if user hasn't scrolled, animate wrapper to `scrollLeft = 120px` then back over 0.8s — peek animation reveals "Dating | Playlist..." before snapping back; gesture education without text instruction

**Artifact:** `DEPLOY/artifacts/brgen/BA304.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
