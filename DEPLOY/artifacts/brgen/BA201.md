# BA201

**Spec:** BA201 Arrow indicator: `<div data-landing-target="arrow" class="nav-arrow">` positioned `top: clamp(20px, 4vw, 32px); right: clamp(20px, 4vw, 32px); width: 28px; height: 28px; color: rgba(255,255,255,0.5)` — SVG chevron-down icon; deliberately dim (50% opacity) — discoverable not screaming

**Artifact:** `DEPLOY/artifacts/brgen/BA201.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
