# BA1005

**Spec:** BA1005 Poll node: `/poll` slash command inserts poll node; renders as `<div data-type="poll">` with editable option fields; on post save, creates `Poll` + `PollOption` records; readers vote via Turbo Stream

**Artifact:** `DEPLOY/artifacts/brgen/BA1005.md`

## Status

Deployable artifact tracked in repo.
