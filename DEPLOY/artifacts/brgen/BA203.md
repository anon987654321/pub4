# BA203

**Spec:** BA203 Arrow pulse: after 3s idle on landing, arrow opacity increases from 0.5 → 0.9 with `transition: opacity 1s` — draws attention without immediately revealing the gesture; resets if user interacts

**Artifact:** `DEPLOY/artifacts/brgen/BA203.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
