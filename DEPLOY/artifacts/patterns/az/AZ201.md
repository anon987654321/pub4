# AZ201

**Spec:** AZ201 Cache-first for shell: `["/", "/offline", "/manifest.webmanifest", "application.js", "application.css"]` in service worker install event precache; app shell always available offline

**Artifact:** `DEPLOY/artifacts/patterns/az/AZ201.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
