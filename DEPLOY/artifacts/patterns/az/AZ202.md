# AZ202

**Spec:** AZ202 Network-first for API: fetch from network; on failure, serve from Cache API if available; update cache on success; `{ cacheName: "api-v1", networkTimeoutSeconds: 3 }`

**Artifact:** `DEPLOY/artifacts/patterns/az/AZ202.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
