# AZ105

**Spec:** AZ105 Declarative link capturing: `"handle_links": "preferred-in-app"` — external links in app open within PWA window rather than system browser; keeps user in app context

**Artifact:** `DEPLOY/artifacts/patterns/az/AZ105.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
