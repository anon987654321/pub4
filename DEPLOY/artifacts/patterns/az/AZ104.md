# AZ104

**Spec:** AZ104 Window controls overlay: `"display_override": ["window-controls-overlay"]` — app content extends into title bar area; add `env(titlebar-area-x/y/width/height)` CSS to position content correctly; desktop PWA feels native

**Artifact:** `DEPLOY/artifacts/patterns/az/AZ104.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
