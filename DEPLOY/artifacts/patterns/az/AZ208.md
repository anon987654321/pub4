# AZ208

**Spec:** AZ208 Offline search: FTS5 index exported as JSON at login; stored in IndexedDB; offline search runs against local index; indicates "offline results" to user; re-sync on reconnect

**Artifact:** `DEPLOY/artifacts/patterns/az/AZ208.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
