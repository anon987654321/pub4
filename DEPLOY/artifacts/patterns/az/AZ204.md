# AZ204

**Spec:** AZ204 IndexedDB schema: `db.createObjectStore("posts", {keyPath: "id"})`; `db.createObjectStore("drafts", {keyPath: "localId", autoIncrement: true})`; `db.createObjectStore("sync_queue", {keyPath: "id", autoIncrement: true})` — structured offline storage

**Artifact:** `DEPLOY/artifacts/patterns/az/AZ204.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
