# AZ207

**Spec:** AZ207 Conflict detection: offline edit + server edit = conflict; on sync, if `server_updated_at > offline_started_at`, show diff to user with merge options; never silently overwrite

**Artifact:** `DEPLOY/artifacts/patterns/az/AZ207.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
