# AZ103

**Spec:** AZ103 Launch handler: `"launch_handler": {"client_mode": "navigate-existing"}` — if app already open, reuse existing window and navigate; prevents duplicate PWA windows

**Artifact:** `DEPLOY/artifacts/patterns/az/AZ103.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
