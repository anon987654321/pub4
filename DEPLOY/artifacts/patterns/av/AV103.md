# AV103

**Spec:** AV103 Path-based routing: single relayd listener routes to different apps by path prefix: `match request path "/amber/*" forward to amber`; eliminates separate subdomains per app where not needed; share TLS cert

**Artifact:** `DEPLOY/artifacts/patterns/av/AV103.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
