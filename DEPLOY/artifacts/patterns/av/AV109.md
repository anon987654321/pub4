# AV109

**Spec:** AV109 Health check: relayd marks backend unhealthy if `/up` returns non-200 three times in 5s; removes from table; traffic routes to remaining healthy instances; email alert via `pflog` + cron

**Artifact:** `DEPLOY/artifacts/patterns/av/AV109.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
