# AV303

**Spec:** AV303 Backup verification: weekly cron job downloads latest Litestream replica and runs `PRAGMA integrity_check` against it; alerts if check fails; never discover backup corruption during a crisis

**Artifact:** `DEPLOY/artifacts/patterns/av/AV303.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
