# AV208

**Spec:** AV208 Soft memory limits: `login.conf` entry for `_<appname>` class sets `memorylocked-cur=512M` and `openfiles-cur=1024`; prevents one app from consuming all VPS memory

**Artifact:** `DEPLOY/artifacts/patterns/av/AV208.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
