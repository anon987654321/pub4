# AV101

**Spec:** AV101 Per-app table: each Rails app gets its own `table <appname> { <server_ip>:<port> }` block in `/etc/relayd.conf`; brgen on 3000, amber on 3001, bsdports on 3002, baibl on 3003, blognet on 3004, hjerterom on 3005

**Artifact:** `DEPLOY/artifacts/patterns/av/AV101.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
