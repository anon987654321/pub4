# AV202

**Spec:** AV202 Service user: each app runs as dedicated `_<appname>` user; `useradd -s /sbin/nologin -d /var/www/<appname> _brgen`; Falcon process starts as this user; never run as root or www

**Artifact:** `DEPLOY/artifacts/patterns/av/AV202.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
