# AV306

**Spec:** AV306 Disk space monitoring: cron checks `df -h /var/www`; alerts at 80% full; auto-purge Active Storage variants older than 30 days (regenerated on demand) if >90% full; never silently fail uploads

**Artifact:** `DEPLOY/artifacts/patterns/av/AV306.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
