# AV110

**Spec:** AV110 Connection buffering: `protocol web { tcp { nodelay } }` — TCP_NODELAY for low-latency WebSocket; `timeout connect 5` — fail fast on unresponsive backend; `timeout read 30` — allow slow streaming responses

**Artifact:** `DEPLOY/artifacts/patterns/av/AV110.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
