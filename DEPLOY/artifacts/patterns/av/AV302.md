# AV302

**Spec:** AV302 Litestream config: `/etc/litestream.yml` — `dbs: - path: /var/db/<appname>/production.sqlite3; replicas: - url: s3://bucket/<appname>/db`; systemd-style service via `rcctl enable litestream`

**Artifact:** `DEPLOY/artifacts/patterns/av/AV302.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
