# AV105

**Spec:** AV105 WebSocket relay: `relay websocket { listen on $ext_addr port 443 tls; table cable; protocol websocket; forward to cable port 28080 }` — ActionCable on separate port; relayd proxies WebSocket upgrade

**Artifact:** `DEPLOY/artifacts/patterns/av/AV105.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
