# AV205

**Spec:** AV205 rcctl enable: `rcctl enable <appname>` in deploy script; `rcctl start <appname>` on first deploy; `rcctl restart <appname>` on subsequent deploys; never kill -9 the Falcon process

**Artifact:** `DEPLOY/artifacts/patterns/av/AV205.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
