# AV201

**Spec:** AV201 rc.d scripts: each app has `/etc/rc.d/<appname>` script implementing `start`, `stop`, `check`, `restart`; uses `daemon` function from `/etc/rc.d/rc.subr`; PID file in `/var/run/<appname>.pid`

**Artifact:** `DEPLOY/artifacts/patterns/av/AV201.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
