# AV102

**Spec:** AV102 relayd relay per app: `relay <appname>_relay { listen on $ext_addr port 443 tls; table <appname>; forward to <appname> port <port> }` — TLS termination at relayd; backend HTTP only; no TLS cert management per app

**Artifact:** `DEPLOY/artifacts/patterns/av/AV102.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
