# AV206

**Spec:** AV206 Environment file: each app reads `/etc/rc.d/<appname>.conf` for `DATABASE_URL`, `RAILS_MASTER_KEY`, `OPENROUTER_API_KEY`; file owned root:_appname, mode 0640; sourced by rc.d script via `. /etc/rc.d/<appname>.conf`

**Artifact:** `DEPLOY/artifacts/patterns/av/AV206.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
