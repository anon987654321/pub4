# AV207

**Spec:** AV207 Shared credentials: RAILS_MASTER_KEY stored in `/etc/master.keys/<appname>` with mode 0400 owned by `_<appname>`; referenced by rc.d script; not in environment on disk in plaintext

**Artifact:** `DEPLOY/artifacts/patterns/av/AV207.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
