# AV203

**Spec:** AV203 Pledge + unveil: Falcon daemon pledges `"stdio rpath wpath cpath inet unix proc exec"`; unveils only app directory, `/tmp`, and database path; `Pledge.pledge` + `Unveil.unveil` called in `config/initializers/pledge.rb`

**Artifact:** `DEPLOY/artifacts/patterns/av/AV203.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
