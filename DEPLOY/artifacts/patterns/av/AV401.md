# AV401

**Spec:** AV401 acme-client for all domains: `acme-client.conf` entry per domain and subdomain; `acme-client -v <domain>` in weekly cron; httpd serves ACME challenges; relayd reloads after cert renewal

**Artifact:** `DEPLOY/artifacts/patterns/av/AV401.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
