# AU201

**Spec:** AU201 Embedding generation job: `GenerateEmbeddingJob.perform_later(record_type, record_id)` — called in `after_commit :generate_embedding, on: [:create, :update]` on embeddable models; never synchronous

**Artifact:** `DEPLOY/artifacts/patterns/au/AU201.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
