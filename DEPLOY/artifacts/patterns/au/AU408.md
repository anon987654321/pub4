# AU408

**Spec:** AU408 Sensitive data scrubbing: before sending any user data to external LLM API, scrub PII — replace email addresses with `[email]`, phone numbers with `[phone]`, account numbers with `[account]`; log scrubbing actions

**Artifact:** `DEPLOY/artifacts/patterns/au/AU408.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
