# AU406

**Spec:** AU406 Prompt versioning: every prompt string stored as constant in `app/prompts/` directory; version-tagged: `OUTFIT_PROMPT_V3 = "..."` — enables A/B testing and rollback; never inline prompts in job code

**Artifact:** `DEPLOY/artifacts/patterns/au/AU406.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
