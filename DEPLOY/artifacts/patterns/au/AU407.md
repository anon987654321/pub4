# AU407

**Spec:** AU407 Context window management: truncate conversation history to last N messages that fit in 75% of context window; reserve 25% for response; compute token counts via `RubyLLM::Tokenizer.count`

**Artifact:** `DEPLOY/artifacts/patterns/au/AU407.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
