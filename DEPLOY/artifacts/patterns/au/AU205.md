# AU205

**Spec:** AU205 Embedding versioning: `embedding_model_version` column on embedding tables; when model changes, queue `ReembedAllJob` which processes in batches; serve old embeddings until re-embed completes

**Artifact:** `DEPLOY/artifacts/patterns/au/AU205.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
