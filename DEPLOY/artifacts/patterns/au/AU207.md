# AU207

**Spec:** AU207 Embedding cache: cache embeddings for queries (not documents) in Solid Cache with 1h TTL; repeated queries (common search terms) skip embedding API call; `Rails.cache.fetch("embed:#{Digest::SHA1.hexdigest(query)}") { embed(query) }`

**Artifact:** `DEPLOY/artifacts/patterns/au/AU207.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
