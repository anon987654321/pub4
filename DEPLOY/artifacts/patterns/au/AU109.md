# AU109

**Spec:** AU109 Cost tracking: `AICall.create(model:, input_tokens:, output_tokens:, cost_ore:, feature:, user_id:, duration_ms:)` after every LLM call; daily cost report in admin dashboard; per-user budget enforcement

**Artifact:** `DEPLOY/artifacts/patterns/au/AU109.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
