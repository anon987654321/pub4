# AU305

**Spec:** AU305 amber: Outfit generation — `POST /ai/outfit` with `{occasion, weather, mood}` → LLM receives wardrobe item summaries + constraints → returns 3 outfit combinations as arrays of item IDs → rendered immediately

**Artifact:** `DEPLOY/artifacts/patterns/au/AU305.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
