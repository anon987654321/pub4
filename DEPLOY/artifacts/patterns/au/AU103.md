# AU103

**Spec:** AU103 Streaming responses: `RubyLLM.chat.ask(prompt) { |chunk| ActionCable.server.broadcast("user_#{user_id}", {chunk: chunk.content}) }` — stream LLM response directly to browser via CableReady; eliminates polling

**Artifact:** `DEPLOY/artifacts/patterns/au/AU103.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
