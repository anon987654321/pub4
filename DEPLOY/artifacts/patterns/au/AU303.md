# AU303

**Spec:** AU303 brgen: Personalized feed ranking — user's engagement history → LLM-generated interest vector → dot-product with post embedding → ranked feed; computed nightly per user; stored in `user_interests` JSON

**Artifact:** `DEPLOY/artifacts/patterns/au/AU303.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
