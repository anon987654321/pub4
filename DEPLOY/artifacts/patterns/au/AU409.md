# AU409

**Spec:** AU409 Output validation: every LLM JSON response parsed through strict schema validator (Dry::Schema or similar); rejected responses logged + retried once with correction instruction in context

**Artifact:** `DEPLOY/artifacts/patterns/au/AU409.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
