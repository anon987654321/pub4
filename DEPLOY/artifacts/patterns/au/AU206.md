# AU206

**Spec:** AU206 Hybrid search implementation: `query_embedding = embed(query)` then `SELECT id, (bm25_score * 0.4 + cosine_similarity * 0.6) AS hybrid_score FROM posts JOIN posts_fts ... ORDER BY hybrid_score DESC LIMIT 20` — RRF blend of keyword + semantic

**Artifact:** `DEPLOY/artifacts/patterns/au/AU206.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
