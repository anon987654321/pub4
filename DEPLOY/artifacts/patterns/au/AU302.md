# AU302

**Spec:** AU302 brgen: AI content moderation — `ModerateContentJob` checks post against NSFW/spam/hate classifiers; returns `{score: 0.1, categories: []}` as JSON; auto-hide if score > 0.8

**Artifact:** `DEPLOY/artifacts/patterns/au/AU302.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
