# AU203

**Spec:** AU203 Embedding model selection: `text-embedding-3-small` (1536 dims, cheap) for semantic search; `text-embedding-3-large` (3072 dims, expensive) for similarity-sensitive features (amber visual similarity); configurable per feature

**Artifact:** `DEPLOY/artifacts/patterns/au/AU203.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
