# AU301

**Spec:** AU301 brgen: AI post tagging — on post create, `TagPostJob` sends title+body to LLM with system prompt "return 3-5 relevant tags as JSON array"; LLM returns `["oslo", "boligmarked", "leie"]`; auto-attach tags

**Artifact:** `DEPLOY/artifacts/patterns/au/AU301.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
