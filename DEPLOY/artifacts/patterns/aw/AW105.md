# AW105

**Spec:** AW105 Customer Portal: `Stripe::BillingPortal::Session.create(customer: stripe_customer_id, return_url:)` — let users manage subscription (cancel, upgrade, update card) via Stripe portal; zero custom subscription management UI needed

**Artifact:** `DEPLOY/artifacts/patterns/aw/AW105.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
