# AW307

**Spec:** AW307 Tip jar: one-time payment without subscription; `Stripe::PaymentIntent.create(amount:, currency: "nok", metadata: {type: "tip", recipient_id:})`; creator receives 90% after Stripe fees

**Artifact:** `DEPLOY/artifacts/patterns/aw/AW307.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
