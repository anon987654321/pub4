# AW104

**Spec:** AW104 Checkout Session: `Stripe::Checkout::Session.create(mode: "subscription", line_items: [...], success_url:, cancel_url:, customer_email:)` — redirect user to Stripe-hosted checkout; no card data touches our servers

**Artifact:** `DEPLOY/artifacts/patterns/aw/AW104.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
