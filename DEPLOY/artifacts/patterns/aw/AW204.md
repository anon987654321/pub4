# AW204

**Spec:** AW204 Payment method preference: user sets default payment method (Stripe card / Vipps) in account settings; checkout respects preference; both paths update same `subscriptions` table

**Artifact:** `DEPLOY/artifacts/patterns/aw/AW204.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
