# AW306

**Spec:** AW306 Grandfathering: early subscribers locked at founding price; `founding_member: true` flag on subscription; never retroactively raise price on grandfathered users; honor indefinitely

**Artifact:** `DEPLOY/artifacts/patterns/aw/AW306.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
