# AW102

**Spec:** AW102 Webhook endpoint: `POST /stripe/webhooks` verified via `Stripe::Webhook.construct_event(payload, sig_header, secret)` — never process Stripe events without signature verification; `protect_from_forgery except: :webhook`

**Artifact:** `DEPLOY/artifacts/patterns/aw/AW102.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
