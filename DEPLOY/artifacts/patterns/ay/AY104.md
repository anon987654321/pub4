# AY104

**Spec:** AY104 Hash-matching: `PhotoDNAJob` computes perceptual hash of all uploaded images; matches against known CSAM hash database (provided by IWF); instant removal + law enforcement report on match

**Artifact:** `DEPLOY/artifacts/patterns/ay/AY104.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
