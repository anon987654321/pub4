# AY103

**Spec:** AY103 AI pre-moderation: every new post and listing passes through `ClassifyContentJob`; if `nsfw_score > 0.7` or `spam_score > 0.8`, auto-flag for review; human moderator reviews; AI never auto-removes

**Artifact:** `DEPLOY/artifacts/patterns/ay/AY103.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
