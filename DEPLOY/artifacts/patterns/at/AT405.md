# AT405

**Spec:** AT405 JSONB columns for flexibility: `add_column :users, :preferences, :json, default: {}` — store user settings (notification_types, feed_density, theme) without schema migrations for each new preference

**Artifact:** `DEPLOY/artifacts/patterns/at/AT405.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
