# AT410

**Spec:** AT410 Covering indexes: `add_index :notifications, [:user_id, :read_at, :created_at]` — includes all columns needed for `SELECT COUNT(*) WHERE user_id = ? AND read_at IS NULL`; zero table access needed

**Artifact:** `DEPLOY/artifacts/patterns/at/AT410.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
