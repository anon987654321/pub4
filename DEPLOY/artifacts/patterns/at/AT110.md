# AT110

**Spec:** AT110 notifications: `notifications(user_id, type, actor_id, notifiable_type, notifiable_id, read_at, created_at)` — polymorphic notifiable; `add_index :notifications, [:user_id, :read_at, :created_at]` for unread feed

**Artifact:** `DEPLOY/artifacts/patterns/at/AT110.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
