# AT302

**Spec:** AT302 blogs: `blogs(user_id, name, slug, description, about_html, plan: {free/pro/business}, subscriber_count, monthly_revenue_ore, custom_domain, verified_at, suspended_at)` — `slug` globally unique; plan determines paywall and newsletter features

**Artifact:** `DEPLOY/artifacts/patterns/at/AT302.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
