# AT304

**Spec:** AT304 newsletter_sends: `newsletter_sends(post_id, blog_id, started_at, completed_at, recipient_count, open_count, click_count, bounce_count)` — analytics per send; not per recipient (privacy); aggregated only

**Artifact:** `DEPLOY/artifacts/patterns/at/AT304.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
