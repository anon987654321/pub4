# AT206

**Spec:** AT206 wear_logs: `wear_logs(item_id, user_id, worn_on, outfit_id, weather, occasion, notes)` — per-item wear history; `add_index :wear_logs, [:item_id, :worn_on]`; CPW = purchase_price / wear_logs.count

**Artifact:** `DEPLOY/artifacts/patterns/at/AT206.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
