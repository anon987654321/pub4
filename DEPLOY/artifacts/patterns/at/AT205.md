# AT205

**Spec:** AT205 declutter_sessions: `declutter_sessions(user_id, started_at, completed_at, items_kept, items_donated, items_sold, challenge_type)` — track declutter campaign progress; items_donated + items_sold for sustainability impact report

**Artifact:** `DEPLOY/artifacts/patterns/at/AT205.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
