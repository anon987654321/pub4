# AT106

**Spec:** AT106 dating likes: `likes(liker_id, liked_id, kind: {like/superlike/pass}, created_at)` — `add_index :likes, [:liker_id, :liked_id], unique: true`; match detection: `SELECT * FROM likes WHERE liker_id = B AND liked_id = A AND kind != 'pass'`

**Artifact:** `DEPLOY/artifacts/patterns/at/AT106.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
