# AT101

**Spec:** AT101 posts table indexes: `add_index :posts, [:community_id, :created_at]`; `add_index :posts, [:user_id, :created_at]`; `add_index :posts, :trending_score`; `add_index :posts, [:pinned, :created_at]` — composite indexes match actual query patterns

**Artifact:** `DEPLOY/artifacts/patterns/at/AT101.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
