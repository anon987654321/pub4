# AT406

**Spec:** AT406 Generated columns: `add_column :posts, :body_length, :integer, as: "length(body)", stored: true` — database computes and indexes derived values; zero application code needed; always consistent

**Artifact:** `DEPLOY/artifacts/patterns/at/AT406.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
