# AT409

**Spec:** AT409 Partial indexes: `add_index :posts, :created_at, where: "status = 'published'"` — index only rows matching predicate; 10× smaller index on posts table with many drafts; matches queries exactly

**Artifact:** `DEPLOY/artifacts/patterns/at/AT409.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
