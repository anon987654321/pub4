# AT105

**Spec:** AT105 dating profiles: `profiles(user_id, bio, birth_date, gender, seeking, city_id, lat, lng, last_active_at, photos_count, verified_at)` — `lat/lng` for distance queries; `last_active_at` for "active recently" filter; `verified_at` for photo verification

**Artifact:** `DEPLOY/artifacts/patterns/at/AT105.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
