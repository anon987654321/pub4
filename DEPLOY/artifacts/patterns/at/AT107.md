# AT107

**Spec:** AT107 matches: `matches(user_a_id, user_b_id, matched_at, conversation_id)` — always `user_a_id < user_b_id` to avoid duplicates; `add_index :matches, [:user_a_id, :user_b_id], unique: true`

**Artifact:** `DEPLOY/artifacts/patterns/at/AT107.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
