# AT305

**Spec:** AT305 reading_history: `reading_history(user_id, post_id, started_at, completed_at, progress_pct, device_type)` — completed_at null = in progress; progress_pct for scroll depth; `add_index :reading_history, [:user_id, :post_id], unique: true`

**Artifact:** `DEPLOY/artifacts/patterns/at/AT305.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
