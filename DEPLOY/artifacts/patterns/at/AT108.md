# AT108

**Spec:** AT108 marketplace listings: `listings(user_id, category_id, title, description, price_ore, currency, condition, status, lat, lng, city_id, views_count, expires_at)` — price in øre (integer); never float for money; `expires_at` for auto-archival

**Artifact:** `DEPLOY/artifacts/patterns/at/AT108.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
