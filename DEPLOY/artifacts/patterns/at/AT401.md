# AT401

**Spec:** AT401 Soft delete: `add_column :table, :deleted_at, :datetime` + `default_scope { where(deleted_at: nil) }` + `def soft_delete; update(deleted_at: Time.current); end` — never hard delete user-generated content immediately; 30-day grace period

**Artifact:** `DEPLOY/artifacts/patterns/at/AT401.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
