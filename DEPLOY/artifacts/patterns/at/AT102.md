# AT102

**Spec:** AT102 posts full-text: `add_column :posts, :search_vector, :virtual, as: "title || ' ' || coalesce(body, '')", stored: true` + FTS5 virtual table over search_vector — avoids double-storing text

**Artifact:** `DEPLOY/artifacts/patterns/at/AT102.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
