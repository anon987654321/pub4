# AT103

**Spec:** AT103 votes denormalization: `add_column :posts, :vote_score, :integer, default: 0, null: false` + `add_column :posts, :comment_count, :integer, default: 0, null: false` — counter caches; avoid COUNT(*) on every render

**Artifact:** `DEPLOY/artifacts/patterns/at/AT103.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
