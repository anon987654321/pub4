# AT202

**Spec:** AT202 outfits: `outfits(user_id, name, occasion, weather_min, weather_max, rating, worn_count, last_worn_at, notes)` + `outfit_items(outfit_id, item_id, position, layer: integer)` — position for display order; layer for layering (base/mid/outer)

**Artifact:** `DEPLOY/artifacts/patterns/at/AT202.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
