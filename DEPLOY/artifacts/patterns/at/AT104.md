# AT104

**Spec:** AT104 follows graph: `follows(follower_id, followee_id, followee_type, created_at)` — polymorphic; `add_index :follows, [:follower_id, :followee_type, :followee_id], unique: true` prevents duplicate follows

**Artifact:** `DEPLOY/artifacts/patterns/at/AT104.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
