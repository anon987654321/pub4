# AT303

**Spec:** AT303 subscriptions: `subscriptions(subscriber_id, blog_id, plan: {free/paid}, status: {active/cancelled/past_due}, stripe_subscription_id, current_period_end, created_at)` — `add_index :subscriptions, [:subscriber_id, :blog_id], unique: true`

**Artifact:** `DEPLOY/artifacts/patterns/at/AT303.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
