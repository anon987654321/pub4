# AX106

**Spec:** AX106 hreflang: `<link rel="alternate" hreflang="nb" href="...">` + `<link rel="alternate" hreflang="en" href="...">` on pages with both language versions; signals to Google which version to show per region

**Artifact:** `DEPLOY/artifacts/patterns/ax/AX106.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
