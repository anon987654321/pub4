# AX304

**Spec:** AX304 JSON feed: `GET /feed.json` returns JSON Feed 1.1 spec; easier to parse than Atom for apps; includes `author`, `content_html`, `image`, `tags`; blognet only

**Artifact:** `DEPLOY/artifacts/patterns/ax/AX304.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
