# AX105

**Spec:** AX105 Description formula: first 150 characters of body_text (plain text, no HTML); fallback to subtitle; never repeat title in description

**Artifact:** `DEPLOY/artifacts/patterns/ax/AX105.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
