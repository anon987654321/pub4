# AX102

**Spec:** AX102 Dynamic OG images: generate OG image per post via `Vips::Image` — post title over background with branding; serve as Active Storage attachment; cache 24h; `og:image` points to static file not dynamic route

**Artifact:** `DEPLOY/artifacts/patterns/ax/AX102.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
