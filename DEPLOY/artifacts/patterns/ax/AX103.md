# AX103

**Spec:** AX103 Canonical URLs: `set_meta_tags canonical: post_url(@post)` on all content pages; prevents duplicate content penalty from `?page=`, `?sort=`, and other query params

**Artifact:** `DEPLOY/artifacts/patterns/ax/AX103.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
