# AX303

**Spec:** AX303 Atom feed: `GET /feed.atom` returns Atom 1.0 feed of latest posts; `format.atom { render layout: false }` in PostsController; `link_to_atom_feed` in head layout; enables RSS readers

**Artifact:** `DEPLOY/artifacts/patterns/ax/AX303.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
