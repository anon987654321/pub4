# AX104

**Spec:** AX104 Title formula: `[Post Title] — [Publication Name] — [App Name]`; max 60 characters; truncate post title if needed; consistent across all apps

**Artifact:** `DEPLOY/artifacts/patterns/ax/AX104.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
