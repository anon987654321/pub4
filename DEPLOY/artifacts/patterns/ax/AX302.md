# AX302

**Spec:** AX302 Sitemap index: root `/sitemap.xml` indexes per-section sitemaps (`/sitemap-posts.xml`, `/sitemap-marketplace.xml`); each sitemap max 50,000 URLs; avoids Google indexation lag

**Artifact:** `DEPLOY/artifacts/patterns/ax/AX302.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
