# AX107

**Spec:** AX107 Robots.txt: allow all crawlers on public content; disallow `/admin`, `/api`, `/account`, `/dating` (private); disallow search result pages (`?q=`); auto-generated from Rails route constraints

**Artifact:** `DEPLOY/artifacts/patterns/ax/AX107.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
