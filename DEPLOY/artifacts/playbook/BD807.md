# BD807

**Spec:** BD807 GPU acceleration via Vips: `Vips::Operation.block_untrusted` ensures safe operations only; `Vips.get("vips-concurrency") = 4` aligns with Falcon worker count; `--gpu` flag enables Vips CUDA path if libvips compiled with CUDA (not OpenBSD)

**Artifact:** `DEPLOY/artifacts/playbook/BD807.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
