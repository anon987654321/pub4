# BD708

**Spec:** BD708 Lens vignette: `VignettePipeline` generates smooth radial mask `1 - (r/R)^2.5 * strength`; multiplied onto image; `--vignette 0.35` is default for all stocks; shape option `--vignette-shape oval|circular`

**Artifact:** `DEPLOY/artifacts/playbook/BD708.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
