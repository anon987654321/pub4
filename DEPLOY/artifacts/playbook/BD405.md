# BD405

**Spec:** BD405 MASTER tool contract: `reach/repligen_tool.rb` wraps repligen CLI as MASTER tool; accepts `{prompt:, chain: "masterpiece"|"quick", style:, budget_usd:}`; returns `{output_path:, cost_usd:, duration_ms:}`; MASTER can autonomously generate images when asked

**Artifact:** `DEPLOY/artifacts/playbook/BD405.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
