# BD804

**Spec:** BD804 Regression test suite: `postpro test` runs all stocks against 5 reference images (portrait, landscape, product, street, night); compares outputs against golden files (perceptual hash threshold <8); fails CI if stock behaviour changed unintentionally

**Artifact:** `DEPLOY/artifacts/playbook/BD804.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
