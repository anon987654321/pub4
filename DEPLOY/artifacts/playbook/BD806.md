# BD806

**Spec:** BD806 Grain optimisation: grain currently generated fresh per image; cache grain texture for same `(width, height, sigma, seed)` tuple in `~/.cache/postpro/grain/`; 3× faster batch processing when same grain params reused across images

**Artifact:** `DEPLOY/artifacts/playbook/BD806.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
