# BD409

**Spec:** BD409 Preview in MASTER CLI: on image generation, MASTER CLI outputs sixel inline image if `$TERM` supports it (`xterm-kitty`, `iTerm2`); else outputs file path and opens with `xdg-open`/`open`; `preview_image` helper in `voice/renderer.rb`

**Artifact:** `DEPLOY/artifacts/playbook/BD409.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
