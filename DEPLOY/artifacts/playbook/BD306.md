# BD306

**Spec:** BD306 Batch prompts from file: `repligen batch flux prompts.txt --out ~/out/` reads one prompt per line; generates all; outputs named `001.png`, `002.png`...; progress bar via `tty-progressbar`; rate-limited to 10 concurrent

**Artifact:** `DEPLOY/artifacts/playbook/BD306.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
