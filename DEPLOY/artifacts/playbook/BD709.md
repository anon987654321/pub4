# BD709

**Spec:** BD709 Dust and scratch: `--dust` overlays semi-transparent scratch texture (pre-computed PNG at 2048×2048 in `data/textures/dust.png`); random offset and rotation per image; adds physicality to AI-generated images

**Artifact:** `DEPLOY/artifacts/playbook/BD709.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
