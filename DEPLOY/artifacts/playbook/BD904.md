# BD904

**Spec:** BD904 Variant caching: processed variant stored as separate Active Storage blob; original preserved; `image_tag photo.processed_variant` served from Cloudflare cache; re-process only on stock/preset change via `postprocessed_preset` column

**Artifact:** `DEPLOY/artifacts/playbook/BD904.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
