# BD910

**Spec:** BD910 Repligen→postpro→brgen pipeline: `SeedGenerationJob` orchestrates: (1) repligen generates `n` images per city; (2) postpro applies city stock preset; (3) Active Storage import attaches to seed posts; (4) posts published to city feed; fully automated city seeding

**Artifact:** `DEPLOY/artifacts/playbook/BD910.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
