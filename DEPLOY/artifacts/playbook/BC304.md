# BC304

**Spec:** BC304 Launch checklist: DNS A record, TLS cert covers wildcard, City record created, seed job run, rc.d relayd config verified, smoke test `curl -I https://losangeles.citynet.no` → 200, announce in r/cityname post linking to new site

**Artifact:** `DEPLOY/artifacts/playbook/BC304.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
