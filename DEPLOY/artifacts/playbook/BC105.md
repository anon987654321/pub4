# BC105

**Spec:** BC105 City admin: `/admin/cities` CRUD; only `role: superadmin` accesses; per-city settings: `open_registration bool`, `moderation_level enum`, `featured_verticals json`; city toggle for verticals (Bergen has all 6, smaller cities may launch with just Regular + Chat)

**Artifact:** `DEPLOY/artifacts/playbook/BC105.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
