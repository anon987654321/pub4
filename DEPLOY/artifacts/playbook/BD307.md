# BD307

**Spec:** BD307 Prompt templates: `~/.config/repligen/prompts/portrait.txt` with `{subject}` placeholder; `repligen gen --template portrait subject="a Norwegian fisherman"` expands template; reusable prompt engineering

**Artifact:** `DEPLOY/artifacts/playbook/BD307.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
