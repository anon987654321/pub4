# BD410

**Spec:** BD410 brgen post from generation: `repligen post --city bergen "prompt"` → generates image → postpro → creates brgen post via API with generated image attached; full seed automation in one command

**Artifact:** `DEPLOY/artifacts/playbook/BD410.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
