# BD605

**Spec:** BD605 Halation simulation: light bleed from bright areas into shadows in film; implement as: `highlights = image.more_than(220)` → Gaussian blur radius 12 → tint `rgba(255, 120, 80, 0.25)` → `screen` blend onto original; toggle via `halation: true` in stock definition

**Artifact:** `DEPLOY/artifacts/playbook/BD605.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
