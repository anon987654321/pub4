# BD210

**Spec:** BD210 Provider abstraction: `ModelProvider` base class with `ReplicateProvider`, `HuggingFaceProvider` (future), `FALProvider` (future) subclasses; same `generate(prompt:, params:)` interface; routing selects provider by model prefix; no lock-in to Replicate

**Artifact:** `DEPLOY/artifacts/playbook/BD210.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
