# BD702

**Spec:** BD702 Selective sharpening: sharpen only mid-frequency detail (not grain); implement as `unsharp_mask(sigma: 1.5, amount: 0.6) - unsharp_mask(sigma: 0.5, amount: 0.6)` to avoid sharpening noise; applied before grain addition

**Artifact:** `DEPLOY/artifacts/playbook/BD702.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
