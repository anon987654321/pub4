# BD403

**Spec:** BD403 Listing photo generation: for seed marketplace listings, `repligen chain masterpiece "product: {title}, clean white background, e-commerce photography"` generates listing photos; metadata written to `listing.photos` via Active Storage import

**Artifact:** `DEPLOY/artifacts/playbook/BD403.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
