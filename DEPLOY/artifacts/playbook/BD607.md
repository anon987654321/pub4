# BD607

**Spec:** BD607 Faded vintage: `--faded` flag; raises blacks by 15 (lifts shadows), reduces contrast by 10%, adds slight warm yellow to shadows (`shadow_tint: [255, 245, 220, 0.08]`); Instagram-era aesthetic on demand

**Artifact:** `DEPLOY/artifacts/playbook/BD607.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
