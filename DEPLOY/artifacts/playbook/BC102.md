# BC102

**Spec:** BC102 TLS wildcard cert: `acme-client` with Domeneshop DNS-01 challenge (API-based); single `*.citynet.no` cert covers all city subdomains without per-city cert renewal; stored at `/etc/ssl/citynet.no.crt` + `/etc/ssl/private/citynet.no.key`

**Artifact:** `DEPLOY/artifacts/playbook/BC102.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
