# BD805

**Spec:** BD805 Performance profiling: `--profile` flag wraps each pipeline step in `Process.clock_gettime` measurement; reports per-step time in manifest; identifies bottlenecks (grain simulation is typically 60% of runtime)

**Artifact:** `DEPLOY/artifacts/playbook/BD805.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
