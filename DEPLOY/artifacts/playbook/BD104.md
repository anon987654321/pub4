# BD104

**Spec:** BD104 Database migrations: introduce `db/migrate/` pattern for repligen SQLite schema; `SchemaVersion` table tracks applied migrations; eliminates `CREATE TABLE IF NOT EXISTS` fragility; new columns added cleanly

**Artifact:** `DEPLOY/artifacts/playbook/BD104.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
