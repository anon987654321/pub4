# BD905

**Spec:** BD905 Progress feedback: Stimulus `upload-controller.js` shows upload progress → "bearbeides..." spinner while `PostproJob` runs → Turbo Stream swaps preview when done; user sees live transition from raw to processed

**Artifact:** `DEPLOY/artifacts/playbook/BD905.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
