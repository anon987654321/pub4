# BD208

**Spec:** BD208 LoRA discovery: separate `loras` table for fine-tuned models; `repligen lora search "anime portrait"` queries Replicate LoRA collection; `repligen lora attach <base_model> <lora>` creates LoRA-applied prediction config; stored as chain template

**Artifact:** `DEPLOY/artifacts/playbook/BD208.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
