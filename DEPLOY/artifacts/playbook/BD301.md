# BD301

**Spec:** BD301 YAML chain definitions: move `CHAIN_TEMPLATES` from hardcoded Ruby hash to `~/.config/repligen/chains/` YAML files; `repligen chain list`, `chain run`, `chain edit`; user-editable without touching source

**Artifact:** `DEPLOY/artifacts/playbook/BD301.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
