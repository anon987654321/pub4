# BC301

**Spec:** BC301 Single brgen process: all cities run in one Rails process; `acts_as_tenant` tenant-switches per request; no per-city processes needed; horizontal scale = add more Puma/Falcon workers, not more processes

**Artifact:** `DEPLOY/artifacts/playbook/BC301.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
