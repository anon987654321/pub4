# BD502

**Spec:** BD502 Split STOCKS constant: `STOCKS` is a large constant inline in the file; extract to `data/film_stocks.yml`; `Stocks.load` reads YAML; allows user-defined custom stocks without editing source code

**Artifact:** `DEPLOY/artifacts/playbook/BD502.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
