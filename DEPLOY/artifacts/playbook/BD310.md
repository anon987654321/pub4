# BD310

**Spec:** BD310 Seed pinning: `--seed 42` pins Replicate prediction seed for reproducibility; stored in prediction record; `repligen vary <prediction_id>` regenerates with same seed ±10 — explores prompt neighbourhood deterministically

**Artifact:** `DEPLOY/artifacts/playbook/BD310.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
