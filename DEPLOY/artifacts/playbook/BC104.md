# BC104

**Spec:** BC104 City model: `City(name, slug, country_code, latitude, longitude, timezone, locale, currency, launch_date, seed_status)`; slug = subdomain; `acts_as_tenant` keys on `city.id`; city record created before launch; seed_status: unseeded → seeded → live

**Artifact:** `DEPLOY/artifacts/playbook/BC104.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
