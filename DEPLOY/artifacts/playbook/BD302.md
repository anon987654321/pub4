# BD302

**Spec:** BD302 Chain dry-run: `repligen chain run masterpiece --dry-run "a foggy Oslo street"` prints planned steps with estimated cost and time without executing; confirms budget before committing to a $2 chain

**Artifact:** `DEPLOY/artifacts/playbook/BD302.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
