# BD503

**Spec:** BD503 Pipeline class: `PostproPipeline.new(image_path, stock:, preset:)` with `#call` returning `Result.ok(output_path)`; replaces imperative script with composable pipeline; each step is a named method with single responsibility

**Artifact:** `DEPLOY/artifacts/playbook/BD503.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
