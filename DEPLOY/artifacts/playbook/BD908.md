# BD908

**Spec:** BD908 Moderation-safe processing: postpro does NOT alter image content (no removal of objects, no face editing); purely colour/grain; safe from "altered evidence" concerns if photos used in reports; documented in privacy policy

**Artifact:** `DEPLOY/artifacts/playbook/BD908.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
