# BC201

**Spec:** BC201 Seed job: `CitySeedJob(city_id)` — creates 20 seed users, 100 posts across 5 content categories, 5 community guidelines posts pinned at top, 3 local business listings; runs once at `seed_status: unseeded → seeded`

**Artifact:** `DEPLOY/artifacts/playbook/BC201.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
