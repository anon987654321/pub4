# BD707

**Spec:** BD707 Chromatic aberration: `--ca` flag; lateral CA simulation — red channel shifted `+0.3px` right, blue `-0.3px` left via `affine`; subtle optical character; stronger on edges (distance from centre weighted); off by default

**Artifact:** `DEPLOY/artifacts/playbook/BD707.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
