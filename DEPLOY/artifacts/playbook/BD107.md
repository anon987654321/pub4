# BD107

**Spec:** BD107 Prediction persistence: store every prediction in `predictions` table `(id, model_id, input_json, output_json, status, cost_usd, duration_ms, created_at)`; enables cost tracking, retry failed predictions, audit trail

**Artifact:** `DEPLOY/artifacts/playbook/BD107.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
