# BD406

**Spec:** BD406 Cost guard in MASTER: MASTER tool contract enforces `budget_usd <= 0.50` per single generation call; above that requires `MASTER_UNSAFE_PROCESS_DEFAULTS=1`; prevents runaway generation costs in autonomous loops

**Artifact:** `DEPLOY/artifacts/playbook/BD406.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
