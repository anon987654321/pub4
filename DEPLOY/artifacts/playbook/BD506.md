# BD506

**Spec:** BD506 Watch mode: `postpro watch ~/Downloads/ --preset social` uses `Listen` gem to detect new `.jpg/.png/.webp` files; auto-processes on write; outputs to `~/Downloads/processed/`; useful for photographer tethered-capture workflows

**Artifact:** `DEPLOY/artifacts/playbook/BD506.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
