# BD206

**Spec:** BD206 Usage analytics: `repligen stats --this-month` reports: total runs, total cost, cost by model, cost by chain type, average generation time, success rate; exported as JSON or pretty table; aids budget planning

**Artifact:** `DEPLOY/artifacts/playbook/BD206.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
