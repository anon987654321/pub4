# BD401

**Spec:** BD401 Seed pipeline rake task: `rake brgen:seed:photos[city_slug,count]` calls repligen to generate `count` photos for city; uses city-specific prompt styles (`Bergen: "Norwegian fjord town, overcast Nordic light"`); outputs to `tmp/seed_photos/<city>/`

**Artifact:** `DEPLOY/artifacts/playbook/BD401.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
