# BD610

**Spec:** BD610 Per-channel curve editor: `postpro curve input.jpg` opens ASCII curve editor (tty-prompt matrix); user adjusts R/G/B S-curve control points interactively; saves named curve preset to `~/.config/postpro/curves/`; applies to batch

**Artifact:** `DEPLOY/artifacts/playbook/BD610.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
