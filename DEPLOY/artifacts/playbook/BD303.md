# BD303

**Spec:** BD303 Chain branching: chain step can specify `branches: 3` — runs 3 parallel variants; user picks best at end; selection stored as `selected_variant` in prediction record; winner fed to next step; creative exploration workflow

**Artifact:** `DEPLOY/artifacts/playbook/BD303.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
