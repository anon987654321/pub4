# BD507

**Spec:** BD507 Vips memory tuning: `Vips.cache_max_mem = 512 * 1024 * 1024` (512MB); `Vips.cache_max = 0` (disable op cache for batch, keeps memory predictable); explicit `image.destroy` after each file in batch; prevents OOM on large batches

**Artifact:** `DEPLOY/artifacts/playbook/BD507.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
