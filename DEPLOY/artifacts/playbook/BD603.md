# BD603

**Spec:** BD603 Add Polaroid 600: strong vignette hardwired; colour bleed simulation via box-blur ×3 on chroma channel before matrix; `matrix: [1.06, -0.04, -0.02, ...]` warm shift; border rendering (white rectangle via `Vips::Image.black(w+80, h+100)` composite)

**Artifact:** `DEPLOY/artifacts/playbook/BD603.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
