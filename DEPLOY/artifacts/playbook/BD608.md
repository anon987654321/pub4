# BD608

**Spec:** BD608 LUT support: `--lut path/to/identity.cube` loads 3D LUT (32×32×32 or 64×64×64); applies via trilinear interpolation in pure Ruby (Vips does not natively load `.cube`); `lut_to_vips_lut` converter; standard DaVinci/Resolve LUTs work

**Artifact:** `DEPLOY/artifacts/playbook/BD608.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
