# BD808

**Spec:** BD808 Memory-mapped input: for images >50MP, `Vips::Image.new_from_file(path, access: :sequential)` streams pixels instead of loading fully; prevents 2GB+ RAM spikes on large repligen outputs (SDXL at 2048×2048 = 12MB but upscaled 4× = 192MB)

**Artifact:** `DEPLOY/artifacts/playbook/BD808.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
