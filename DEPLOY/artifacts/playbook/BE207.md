# BE207

**Spec:** BE207 AI summaries of hot threads: on threads with >20 replies, "Vis sammendrag" button → MASTER summarises thread in 3 sentences via streaming Turbo Stream; X has Grok summaries (US only, paid); Facebook has no equivalent; brgen's is free and local-language-aware

**Artifact:** `DEPLOY/artifacts/playbook/BE207.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
