# BD704

**Spec:** BD704 Sky detection and enhancement: `sky_mask = image.band(1).more_than(image.band(2))` (blue channel dominates) + luminance filter; within sky mask: slight gradient from warmer horizon to cooler zenith; enhances landscape shots from repligen

**Artifact:** `DEPLOY/artifacts/playbook/BD704.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
