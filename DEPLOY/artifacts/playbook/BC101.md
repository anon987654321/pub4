# BC101

**Spec:** BC101 Domain convention: flagship `brgen.no`; other cities follow `<cityname>.citynet.no` pattern; `losangeles.citynet.no`, `amsterdam.citynet.no`, `london.citynet.no`; register `citynet.no` as the parent domain at Domeneshop; wildcard DNS `*.citynet.no → server IP`

**Artifact:** `DEPLOY/artifacts/playbook/BC101.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
