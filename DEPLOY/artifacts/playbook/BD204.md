# BD204

**Spec:** BD204 Favourite models: `user_favourites(model_id, alias, default_params_json)`; `repligen fav add black-forest-labs/flux-schnell --alias flux`; `repligen gen flux "prompt"` expands alias and merges default params; `.repligen_aliases` file in home dir

**Artifact:** `DEPLOY/artifacts/playbook/BD204.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
