# BD903

**Spec:** BD903 Preset selection by context: `dating` profile photos → `preset: portrait` (skin protection, soft grain); `marketplace` listing photos → `preset: product` (clarity, white lift, sharpness); `feed` photos → `preset: social`; preset resolved by controller context

**Artifact:** `DEPLOY/artifacts/playbook/BD903.md`

## Deploy / verify

See `DEPLOY/rails/PRODUCTION_READINESS.md` and `DEPLOY/openbsd/README.md` for VPS steps.

## Status

Deployable artifact tracked in repo. Run verification on OpenBSD target before live cutover.
