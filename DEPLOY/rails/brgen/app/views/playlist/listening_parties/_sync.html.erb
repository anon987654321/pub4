<% if track %>
  <p>Now playing: <strong><%= track.title %></strong> — <%= track.artist %></p>
  <p class="dim">Synced position: <%= party.position_seconds %>s</p>
<% else %>
  <p class="dim">Waiting for the host to pick a track…</p>
<% end %>