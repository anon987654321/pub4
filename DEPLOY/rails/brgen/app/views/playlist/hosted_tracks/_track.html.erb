<article>
  <h2><%= link_to track.title, playlist_hosted_track_path(track) %></h2>
  <p class="dim">
    <%= [track.artist, track.genre, track.duration_formatted].compact.join(" · ") %>
  </p>
</article>