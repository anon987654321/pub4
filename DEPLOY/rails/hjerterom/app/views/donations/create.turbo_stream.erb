<%= turbo_stream.append "donations", partial: "donations/donation", locals: { donation: @donation } %>
<%= turbo_stream.replace "hjerterom-donation-new" do %>
  <turbo-frame id="hjerterom-donation-new">
    <p class="dim">Donation saved. <%= link_to "View", @donation %> or <%= link_to "add another", new_donation_path %>.</p>
  </turbo-frame>
<% end %>