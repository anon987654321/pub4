<% content_for :title, @profile.display_name %>
<%= turbo_stream_from @profile %>

<header class="profile-header" role="banner" aria-label="Creator profile">
  <h1><%= @profile.display_name %></h1>
  <p class="dim">@<%= @profile.handle %></p>
  <% if @profile.bio.present? %>
    <p><%= @profile.bio %></p>
  <% end %>
  <% if authenticated? && Current.user == @profile.user %>
    <%= link_to "Edit profile", edit_my_creator_profile_path, class: "btn btn-sm" %>
  <% elsif @can_remix %>
    <p class="dim">Remix-friendly wardrobe</p>
  <% end %>
</header>

<h2>Showcase</h2>
<% if @showcase_items.any? %>
  <div class="item-grid">
    <% @showcase_items.each do |showcase| %>
      <%= link_to item_path(showcase.item) do %>
        <% if showcase.item.photos.attached? %>
          <%= image_tag showcase.item.photos.first, alt: showcase.item.title %>
        <% else %>
          <div class="item-placeholder"><%= showcase.item.category %></div>
        <% end %>
        <p><%= showcase.caption.presence || showcase.item.title %></p>
      <% end %>
    <% end %>
  </div>
<% else %>
  <p class="dim">No showcase items yet.</p>
<% end %>