name: MASTER Tests

# push/pull_request triggers restored (billing lock cleared).
#
# Local: cd MASTER && bin/check --profile=full
# Or: OPENBSD/bin/check --profile=contributor && cd MASTER && bin/check --profile=agent
on:
  workflow_dispatch:
  push:
    paths:
      - "MASTER/**"
      - ".github/workflows/master-tests.yml"
  pull_request:
    paths:
      - "MASTER/**"
      - ".github/workflows/master-tests.yml"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: .ruby-version

          bundler-cache: true
          working-directory: MASTER

      - name: Run MASTER CI
        working-directory: MASTER
        run: bundle exec bin/ci

  browser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: .ruby-version

          bundler-cache: true
          working-directory: MASTER

      - name: Run MASTER browser CI
        working-directory: MASTER
        env:
          MASTER_CI_BROWSER: "1"
          PROBE_REQUIRE_BROWSER: "1"
        run: bundle exec bin/ci