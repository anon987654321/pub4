```html
<div class="flash flash-<%= flash_type %>" role="alert" aria-live="polite" data-controller="flash">
  <div class="flash-content">
    <%= sanitize(flash_message, tags: %w[p b i u strong em]) %>
  </div>
  <button type="button" class="flash-dismiss" data-action="flash#dismiss" aria-label="Dismiss alert">
    <span aria-hidden="true">&times;</span>
    <span class="visually-hidden">Dismiss this message</span>
  </button>
</div>
```
