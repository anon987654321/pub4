---
name: master.yml + master.json are the constitutional source of truth
description: Current Ruby MASTER must implement what the predecessor master.json (v43.0.1) and master.yml (v31, v49.75, etc.) describe — close the drift
type: project
originSessionId: 038b16d9-fc5e-4144-9a47-5bd746b2d3ac
---

The directive is that MASTER should do what `master.yml` and `master.json` describe. YAML and JSON predecessors are the authoritative spec; Ruby is the executor. This was stated 2026-05-05 after mining deleted `master.json` (November–December 2025, 130 commits) and `master.yml` (December 2025–February 2026, 668 commits).

Priority gaps to close include the six Universal Laws ladder from ROBUSTNESS through SINGULARITY, LINEARITY, PROXIMITY, ABSTRACTION, and DENSITY; Strunk & White safeguards with `apply_to` and `never_apply_to` that never delete vars, calls, or conditionals; a biases section covering hallucination, simulation, completion_theater, sycophancy, false_confidence, and cognitive traps; structural_ops taxonomy; an eight-phase workflow plus learn and introspect; patterns.veto and high regex; five adversarial questions and five to fifteen solutions; fixed-point convergence with two silent runs; incremental scanning; prediction engine and autofix thresholds; twelve weighted council personas with veto rights; SHA256 evidence logging; a beauty section; preserve boot, dmesg, and help; OpenBSD per-config validators; tech stack constants for LCP, INP, CLS, WCAG, touch, and 66ch; cost guards; and per-language templates.

Execute in order; each item should be commit-able. Aesthetic items are first-class for this architect user.