---
schema: 1
defaults:
  max_turns: 80
  max_attempts_per_task: 3
  max_minutes: 60
  context_chars: 18000
autonomy:
  automatic_repair: true
  automatic_resume: true
  validate_yaml_at_boot: true
  checkpoint_before_task: true
  require_evidence_for_completion: true
  never_delegate_to_external_agent: true
