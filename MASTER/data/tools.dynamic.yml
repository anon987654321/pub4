# Dynamic HTTP tools — loaded at boot without code changes.
# Example (disabled):
# - name: status_ping
#   enabled: false
#   method: GET
#   url: "https://ai.brgen.no/health"
#   tier: safe
#   description: Ping production health endpoint