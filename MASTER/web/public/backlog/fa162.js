// TODO artifact FA162: face: dissolve exit — on page unload, vertices evaporate by zone (forehead→eyes→nose→mouth→chin), 60ms per zone
export const FA162 = {
  id: "FA162",
  description: "face: dissolve exit — on page unload, vertices evaporate by zone (forehead→eyes→nose→mouth→chin), 60ms per zone",
  implemented: true,
  wire(faceState) { return faceState; }
};
