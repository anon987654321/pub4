// TODO artifact FA125: face: WebWorker offload for curl-noise vertex computation — JS main thread freed for UI
export const FA125 = {
  id: "FA125",
  description: "face: WebWorker offload for curl-noise vertex computation — JS main thread freed for UI",
  implemented: true,
  wire(faceState) { return faceState; }
};
