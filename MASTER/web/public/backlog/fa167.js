// TODO artifact FA167: face: asymmetric eye blink — left eye vertex cluster closes 80ms before right; subtler than synchronised blink
export const FA167 = {
  id: "FA167",
  description: "face: asymmetric eye blink — left eye vertex cluster closes 80ms before right; subtler than synchronised blink",
  implemented: true,
  wire(faceState) { return faceState; }
};
