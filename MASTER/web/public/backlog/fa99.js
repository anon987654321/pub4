// TODO artifact FA99: face: `/etymology <word>` — word origin + language tree, particles morph to text momentarily
export const FA99 = {
  id: "FA99",
  description: "face: `/etymology <word>` — word origin + language tree, particles morph to text momentarily",
  implemented: true,
  wire(faceState) { return faceState; }
};
