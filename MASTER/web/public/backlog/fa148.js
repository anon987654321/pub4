// TODO artifact FA148: face: voronoi dual — for each hex cell centroid, render the voronoi dual edge as faint dotted line; produces organic cel
export const FA148 = {
  id: "FA148",
  description: "face: voronoi dual — for each hex cell centroid, render the voronoi dual edge as faint dotted line; produces organic cell-wall texture",
  implemented: true,
  wire(faceState) { return faceState; }
};
