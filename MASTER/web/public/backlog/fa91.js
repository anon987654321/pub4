// TODO artifact FA91: face: `/flashcard` mode — face shows term particle-splash, reads definition, waits for recall
export const FA91 = {
  id: "FA91",
  description: "face: `/flashcard` mode — face shows term particle-splash, reads definition, waits for recall",
  implemented: true,
  wire(faceState) { return faceState; }
};
