// TODO artifact FA86: face: haptic feedback on mobile send — navigator.vibrate(30) on submit
export const FA86 = {
  id: "FA86",
  description: "face: haptic feedback on mobile send — navigator.vibrate(30) on submit",
  implemented: true,
  wire(faceState) { return faceState; }
};
