// TODO artifact FA83: face: "focus mode" — hide all UI except face and input, full-screen canvas
export const FA83 = {
  id: "FA83",
  description: "face: \"focus mode\" — hide all UI except face and input, full-screen canvas",
  implemented: true,
  wire(faceState) { return faceState; }
};
