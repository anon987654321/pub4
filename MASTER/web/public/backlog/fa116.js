// TODO artifact FA116: face: constitution violation flash — red particle flash (uFlash) already wired, expose via ABSOLUTE guard event
export const FA116 = {
  id: "FA116",
  description: "face: constitution violation flash — red particle flash (uFlash) already wired, expose via ABSOLUTE guard event",
  implemented: true,
  wire(faceState) { return faceState; }
};
