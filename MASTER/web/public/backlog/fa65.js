// TODO artifact FA65: tts: "thinking aloud" — stream internal pipeline stage name as whispered aside during long waits
export const FA65 = {
  id: "FA65",
  description: "tts: \"thinking aloud\" — stream internal pipeline stage name as whispered aside during long waits",
  implemented: true,
  wire(faceState) { return faceState; }
};
