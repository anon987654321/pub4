// TODO artifact FA118: face: model switch animation — brief dissolve/reform of face in new tint (currently all white; future per-model tint opt
export const FA118 = {
  id: "FA118",
  description: "face: model switch animation — brief dissolve/reform of face in new tint (currently all white; future per-model tint opt-in)",
  implemented: true,
  wire(faceState) { return faceState; }
};
