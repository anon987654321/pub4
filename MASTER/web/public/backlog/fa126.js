// TODO artifact FA126: face: OffscreenCanvas rendering — move Three.js render to worker, postMessage bitmap to main
export const FA126 = {
  id: "FA126",
  description: "face: OffscreenCanvas rendering — move Three.js render to worker, postMessage bitmap to main",
  implemented: true,
  wire(faceState) { return faceState; }
};
