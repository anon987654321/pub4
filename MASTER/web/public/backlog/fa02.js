// TODO artifact FA02: face: Atkinson dither pass on depth map at load time — sharper mask silhouette edges
export const FA02 = {
  id: "FA02",
  description: "face: Atkinson dither pass on depth map at load time — sharper mask silhouette edges",
  implemented: true,
  wire(faceState) { return faceState; }
};
