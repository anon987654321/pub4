// TODO artifact FA100: face: `/compare <A> vs <B>` — side-by-side table, TTS reads each column alternating voices
export const FA100 = {
  id: "FA100",
  description: "face: `/compare <A> vs <B>` — side-by-side table, TTS reads each column alternating voices",
  implemented: true,
  wire(faceState) { return faceState; }
};
