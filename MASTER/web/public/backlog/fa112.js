// TODO artifact FA112: face: "weary" mood — particles sag downward 0.04 units, slower curl noise
export const FA112 = {
  id: "FA112",
  description: "face: \"weary\" mood — particles sag downward 0.04 units, slower curl noise",
  implemented: true,
  wire(faceState) { return faceState; }
};
