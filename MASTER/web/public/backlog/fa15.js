// TODO artifact FA15: face: add second mask layer (overlay) — subtle texture from a different image blended at 0.3 alpha
export const FA15 = {
  id: "FA15",
  description: "face: add second mask layer (overlay) — subtle texture from a different image blended at 0.3 alpha",
  implemented: true,
  wire(faceState) { return faceState; }
};
