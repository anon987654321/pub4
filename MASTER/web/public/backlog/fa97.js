// TODO artifact FA97: face: wrong answer = particles shake (FA-shake mood); correct = bloom (FA-pass mood)
export const FA97 = {
  id: "FA97",
  description: "face: wrong answer = particles shake (FA-shake mood); correct = bloom (FA-pass mood)",
  implemented: true,
  wire(faceState) { return faceState; }
};
