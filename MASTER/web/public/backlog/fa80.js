// TODO artifact FA80: face: typing indicator animation while streaming — 3 particle dots pulse in sequence
export const FA80 = {
  id: "FA80",
  description: "face: typing indicator animation while streaming — 3 particle dots pulse in sequence",
  implemented: true,
  wire(faceState) { return faceState; }
};
