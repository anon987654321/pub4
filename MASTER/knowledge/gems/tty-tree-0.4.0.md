
### Options

- `:level` – maximum depth (default: unlimited).
- `:file_limit` – limit number of entries.
- `:show_hidden` – include hidden files/directories.
- `:only_dirs` – display only directories.
- `:indent` – spaces per nesting level (default: 4).

### Rendering

