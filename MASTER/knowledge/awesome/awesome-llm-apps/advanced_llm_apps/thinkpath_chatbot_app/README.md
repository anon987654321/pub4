# Clone the repository
git clone https://github.com/yourorg/awesome-llm-apps.git
cd awesome-llm-apps/advanced_llm_apps/thinkpath_chatbot_app

# Install dependencies (uses bundler)
bundle install

# Export any required API keys, e.g.:
export OPENROUTER_API_KEY=sk_...

# Run the chatbot
ruby thinkpath_chatbot_app/app.rb
