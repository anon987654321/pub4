#!/usr/bin/env ruby
# frozen_string_literal: true

# Minimal kernel smoke test placeholder to satisfy bin/probe when no kernel specs exist.
# This intentionally does nothing and exits 0.
puts "kernel_smoke: placeholder"
exit 0
