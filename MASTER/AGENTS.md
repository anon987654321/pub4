# Agents

Bootstrap for coding agents lives in Ruby runtime: `/orient bootstrap`, `/orient agents`, and `/orient conventions`. Constitutional law is `data/soul.yml` and `data/rules.yml`. VPS deploy is `DEPLOY/OPERATOR.md`.