# Now

CLI REPL, pipeline stages, command registry, and script dispatch.

- `cli/` — REPL flow, command handlers, result display
- `command_registry/` — slash commands and formatters
- `stages/` — execute, admit, and pipeline context

Entrypoints: `MASTER/bin/cli`, `bin/dogfood`.