# Quickstart

Runtime authority moved to YAML and `/orient`. Use `/orient bootstrap` for the mental model, `/orient soul` and `/orient rules` for law, and `bin/probe` for readiness. VPS operator detail stays in `DEPLOY/OPERATOR.md`.